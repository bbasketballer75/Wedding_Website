---
phase: "11"
plan: "03"
type: execute
wave: 1
depends_on:
  - "11-01"
files_modified:
  - src/pages/Home.tsx
  - src/components/sections/EngagementSection.css
autonomous: true
requirements:
  - UX-04
must_haves:
  truths:
    - "Home.tsx uses no hardcoded hex colors"
    - "EngagementSection.css uses no hardcoded #d4af37 hex"
  artifacts:
    - path: "src/pages/Home.tsx"
      provides: "Home page with design token colors only"
      not_contains: "text-[#|bg-[#|#[0-9a-fA-F]"
    - path: "src/components/sections/EngagementSection.css"
      provides: "EngagementSection with token-based colors"
      not_contains: "#[0-9a-fA-F]{6}"
---

<objective>
Replace hardcoded hex values in Home page sections with design token references.

Purpose: Per UX-04 requirement, eliminate hardcoded hex values in Home page sections. The Home.tsx uses bg-[#1a1208] for the cinematic chapter panel, and EngagementSection.css contains 3 instances of #d4af37.
Output: Home page components using design token references.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/pages/Home.tsx
@src/components/sections/EngagementSection.css
@src/index.css
</context>

<interfaces>
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Replace bg-[#1a1208] in Home.tsx</name>
  <read_first>src/pages/Home.tsx</read_first>
  <action>
    In Home.tsx line 259, replace the hardcoded dark background:
    `className="relative overflow-hidden bg-[#1a1208] py-20 sm:py-28"`
    TO:
    `className="relative overflow-hidden bg-charcoal-900 py-20 sm:py-28"`

    The hex #1a1208 is a very dark brown/mocha - equivalent to charcoal-900 which is #151413 per designTokens.ts. This provides semantic meaning and matches the dark cinematic aesthetic.
  </action>
  <verify>
    <automated>grep -n "#1a1208" src/pages/Home.tsx || echo "PASS: hardcoded hex not found"</automated>
  </verify>
  <done>
    Home.tsx line 259 uses bg-charcoal-900 instead of hardcoded hex
  </done>
  <acceptance_criteria>
    - `grep -n "#1a1208" src/pages/Home.tsx` returns no matches
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Replace #d4af37 in EngagementSection.css</name>
  <read_first>src/components/sections/EngagementSection.css</read_first>
  <action>
    In EngagementSection.css, replace all 3 instances of #d4af37 with var(--color-gold-500) CSS variable reference:

    Line 49 (color property):
    CHANGE: `color: #d4af37;`
    TO: `color: var(--color-gold-500);`

    Line 157 (background-color property):
    CHANGE: `background-color: #d4af37;`
    TO: `background-color: var(--color-gold-500);`

    Line 165 (color property):
    CHANGE: `color: #d4af37;`
    TO: `color: var(--color-gold-500);`

    This uses the CSS variable so the color respects theme switching and stays aligned with the brand gold.
  </action>
  <verify>
    <automated>grep -n "#d4af37" src/components/sections/EngagementSection.css || echo "PASS: hardcoded hex not found"</automated>
  </verify>
  <done>
    EngagementSection.css uses var(--color-gold-500) instead of hardcoded hex
  </done>
  <acceptance_criteria>
    - `grep -n "#d4af37" src/components/sections/EngagementSection.css` returns no matches
    - `grep -n "var(--color-gold-500)" src/components/sections/EngagementSection.css` returns 3 matches
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | CSS class replacement with no security implications |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-03 | N/A | Home.tsx/EngagementSection.css | accept | Color class replacement only |

</threat_model>

<verification>
- `grep -rn "text-\[#\|bg-\[#\|#[0-9a-fA-F]" src/pages/Home.tsx` returns no matches (except gradient syntax)
- `grep -rn "#[0-9a-fA-F]" src/components/sections/EngagementSection.css` returns no matches
- `npm run build` produces no Tailwind validation errors
</verification>

<success_criteria>
1. Home.tsx uses bg-charcoal-900 instead of bg-[#1a1208]
2. EngagementSection.css uses var(--color-gold-500) for all 3 instances
3. Build completes without errors
</success_criteria>

<output>
After completion, update ROADMAP.md phase 11 progress.
</output>
