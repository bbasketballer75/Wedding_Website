---
phase: "18"
plan: "01"
type: "execute"
wave: 1
depends_on: []
files_modified:
  - "supabase/migrations/20260521000100_photo_claiming.sql"
  - "src/lib/supabase.ts"
  - "src/stores/claimStore.ts"
  - "src/pages/Upload.tsx"
  - "src/pages/People.tsx"
  - "src/pages/admin/Dashboard.tsx"
  - "src/components/photo-viewer/PhotoLightbox.tsx"
autonomous: true
requirements:
  - "SC-01"
  - "SC-02"
must_haves:
  truths:
    - "Elegant 'Claim My Photos' banner is present on the Guest Uploads page (/upload)"
    - "Elegant 'Is this you? Claim this face' button is present on the People gallery cards"
    - "Dual-mode OTP verification flow operates with standard Supabase Auth and a Client-Side Developer Fallback Simulation Panel"
    - "Photo claims are persisted in PostgreSQL tables: guest_identities and photo_claims with full RLS security policies"
    - "Admin moderation panel includes a fully operational 'Claims' tab displaying pending claims with approve/reject actions"
    - "Approving/rejecting claims logs entries to moderation_audit_log and updates verified tags in the system"
    - "Approved guest names render in premium gold italics inside the Photo Lightbox info panel"
  artifacts:
    - path: "supabase/migrations/20260521000100_photo_claiming.sql"
      provides: "PostgreSQL migration file creating guest_identities, photo_claims, and audit updates"
    - path: "src/stores/claimStore.ts"
      provides: "Zustand store managing the photo claiming wizard flow and sandbox OTP simulation state"
    - path: "src/lib/supabase.ts"
      provides: "Database access layer for guest identities, claim submissions, and claims admin moderation"
  key_links:
    - from: "Upload.tsx"
      to: "claimStore"
      via: "Mounts claiming modal and binds trigger banner"
    - from: "People.tsx"
      to: "claimStore"
      via: "Binds Face Claim trigger click to set predefined claim details"
    - from: "admin/Dashboard.tsx"
      to: "supabase.ts"
      via: "Fetches and moderates claim requests with audit logging"
---

<objective>
Implement a beautiful, end-to-end self-service photo claiming and identity verification system. Guests will be able to claim photos they uploaded or appear in via a Magic Link or OTP email verification flow with a rich developer simulation sandbox fallback. Admin moderation tools will review claims, sync verified names to Digikam face clusters, and render approved guests in gold italics inside the photo lightbox.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-photo-claiming/18-CONTEXT.md
@.planning/phases/18-photo-claiming/18-UI-SPEC.md
@src/lib/supabase.ts
@src/pages/Upload.tsx
@src/pages/People.tsx
@src/pages/admin/Dashboard.tsx
@src/components/photo-viewer/PhotoLightbox.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Supabase Schema Migration for Photo Claiming</name>
  <files>supabase/migrations/20260521000100_photo_claiming.sql</files>
  <action>
    Create a new database migration file defining the photo claiming schema:
    1. Define public.guest_identities: id (uuid, primary key), email (text, unique), session_id (text), display_name (text), created_at (timestamp).
    2. Define public.photo_claims: id (uuid, primary key), guest_identity_id (uuid, foreign key, cascade delete), photo_id (uuid, foreign key, cascade delete), face_id (uuid, foreign key, set null, optional), claim_type (text: 'upload' or 'face'), status (text: 'pending', 'approved', 'rejected'), rejection_reason (text), created_at (timestamp), updated_at (timestamp).
    3. Add unique constraints to prevent double-claiming the same photo by the same guest identity.
    4. Enable RLS and setup permissive policies:
       - public inserts for claims and identities (guests submit claims)
       - public select for guest_identities (so lightbox can read approved guest names)
       - authenticated full access for admin users.
    5. Add index structures to maximize performance on query matches.
  </action>
  <verify>
    Ensure script is syntactically sound and matches Supabase standards.
  </verify>
  <acceptance_criteria>
    - Tables exist with correct relationships, unique constraints, and foreign keys.
    - Row-level security is enabled and correct policies are configured.
    - Index structures are set on status and identity fields.
  </acceptance_criteria>
  <done>
    Database migration script authored.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement Database Functions in src/lib/supabase.ts</name>
  <files>src/lib/supabase.ts</files>
  <action>
    Integrate claiming database queries into the unified database access layer:
    1. Fetching user's potential uploads by email from `guest_uploads`.
    2. Inserting guest identities and submitting bulk photo/face claims.
    3. Admin claims query methods: `fetchPendingClaims()`, `fetchApprovedClaims()`.
    4. Admin moderation actions: `approvePhotoClaim(claimId)`, `rejectPhotoClaim(claimId, reason)`.
    5. Integrate claims moderation with `moderation_audit_log` tracking actions.
    6. Fetch list of all verified guest identities for gold italics styling.
  </action>
  <verify>
    Ensure TypeScript compilation and typings align cleanly.
  </verify>
  <acceptance_criteria>
    - All database client interface calls compile.
    - Audit records are correctly written inside claim approval/rejection commands.
  </acceptance_criteria>
  <done>
    Database interaction queries and admin commands implemented.
  </done>
</task>

<task type="auto">
  <name>Task 3: Build Zustand claimStore for the Claiming Modal Wizard</name>
  <files>src/stores/claimStore.ts</files>
  <action>
    Build a Zustand store (`src/stores/claimStore.ts`) to manage claiming wizards:
    1. Modal states: open/closed, active wizard step (1: Email & Name, 2: OTP Verification, 3: Confirm Photos, 4: Success).
    2. Input values: name, email, OTP code.
    3. Sandbox OTP simulation state: a boolean `sandboxMode` flag, `simulatedCode` generated randomly (6 digits) when code is requested, and clear console/UI logs.
    4. Claim details: current photo/face targeted by "Is this you?".
    5. Store actions for advancing steps, executing OTP verification, and dispatching claims to Supabase.
  </action>
  <verify>
    Write a complete unit test suite validating the claiming wizard state machine transitions.
  </verify>
  <acceptance_criteria>
    - claimStore state and actions compile cleanly.
    - Simulated OTP matches, verification states, and modal triggers behave predictably.
  </acceptance_criteria>
  <done>
    Zustand claimStore fully implemented and unit tested.
  </done>
</task>

<task type="auto">
  <name>Task 4: Implement claiming triggers and Glassmorphic Wizard Modals</name>
  <files>src/pages/Upload.tsx, src/pages/People.tsx, src/components/gallery/ClaimModal.tsx</files>
  <action>
    Build the frontend claiming interactions:
    1. Create `src/components/gallery/ClaimModal.tsx`: A glassmorphic modal overlay managing the verification questionnaire:
       - Step 1: Input name and email.
       - Step 2: Input 6-digit OTP code, showing blurred preview thumbnails of matching guest uploads behind.
       - Step 2b (Developer Simulation Panel): If sandboxed, display a beautiful developer panel showing "Developer Sandbox: Simulated OTP Code: 123456 [Click to Autocomplete]" for offline debugging.
       - Step 3: Photos selection/preview listing where they can confirm what photos they want to claim.
       - Step 4: Success confirmation toast/message.
    2. Add "Claim My Photos" entry card on `/upload` page styled with gold borders and cream backgrounds.
    3. Add "Is this you? Claim this face" button overlay on `/people` cards.
  </action>
  <verify>
    Verify compile and responsive rendering on small screens.
  </verify>
  <acceptance_criteria>
    - Modal features Framer Motion animations (300ms ease-out).
    - Sandbox developer fallback panel functions seamlessly.
  </acceptance_criteria>
  <done>
    Claim triggers and modal wizard completed.
  </done>
</task>

<task type="auto">
  <name>Task 5: Integrate Claims Moderation in Admin Dashboard</name>
  <files>src/pages/admin/Dashboard.tsx</files>
  <action>
    Implement moderation tools inside the Admin Dashboard:
    1. Add a "Claims" tab in `Dashboard.tsx` alongside other tabs.
    2. Render a beautiful table of pending claim requests showing: Guest Name, Email, Claim Type (Upload / Face), target photo thumbnail, and timestamp.
    3. Provide "Approve Claim" and "Reject Claim" buttons.
    4. Rejections prompt a clean dialog asking for a cancellation/moderation reason.
    5. Hook the actions into the new Supabase admin functions.
  </action>
  <verify>
    Verify compilation and RLS action responses.
  </verify>
  <acceptance_criteria>
    - Admin tab renders cleanly inside the dashboard grid.
    - All tab state switches correctly with counts updated dynamically.
  </acceptance_criteria>
  <done>
    Admin Dashboard photo claiming moderation panel integrated.
  </done>
</task>

<task type="auto">
  <name>Task 6: Highlight Approved Guests in Lightbox Info Panel</name>
  <files>src/components/photo-viewer/PhotoLightbox.tsx</files>
  <action>
    Render verified names elegantly:
    1. Fetch or cache the list of approved guest identities.
    2. In `PhotoLightbox.tsx`, match names listed in `currentPhoto.faces` against the verified/approved names list.
    3. If matched, style the name badge using premium HSL gold highlights, adding custom gold italic font-weights (`text-gold-500 font-medium italic`).
  </action>
  <verify>
    Inspect lightbox styles to ensure correct colors are rendered.
  </verify>
  <acceptance_criteria>
    - Lightbox info badges use consistent border radius (`rounded-full` or `rounded-xl`).
    - Verified names are visually distinct and premium.
  </acceptance_criteria>
  <done>
    Premium gold italic highlights integrated in photo lightbox faces info list.
  </done>
</task>

<task type="auto">
  <name>Task 7: Write and run verification tests</name>
  <files>package.json</files>
  <action>
    Test all newly created logic:
    1. Add unit tests for claims state management and claiming wizard step progression.
    2. Verify overall site responsiveness and zero regressions.
    3. Run the full test suite.
  </action>
  <verify>
    npm run test:run
  </verify>
  <acceptance_criteria>
    - Vitest runs successfully and all tests pass.
  </acceptance_criteria>
  <done>
    QA tests written and verified.
  </done>
</task>

</tasks>

<verification>
Ensure all changes compile and build successfully:
- `npm run build`
Run Vitest automated test suite:
- `npm run test:run`
</verification>

<success_criteria>
- Photos claiming flows smoothly from guest entry points to OTP validation.
- Developer simulation sandbox behaves perfectly offline with autocomplete buttons.
- Admin dashboard manages claims moderation seamlessly with audit trail preservation.
- Approved tags reflect in premium gold italic indicators on lightbox views.
- Clean database migrations and optimized schema structures exist.
</success_criteria>

<output>
Upon completion, author the `.planning/phases/18-photo-claiming/18-01-SUMMARY.md` file summarizing details.
</output>
