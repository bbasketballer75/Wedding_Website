---
phase: "19"
plan: "01"
type: "execute"
wave: 1
depends_on: []
files_modified:
  - "src/components/photo-viewer/PhotoLightbox.tsx"
  - "src/pages/Upload.tsx"
  - "src/App.tsx"
files_created:
  - "src/components/photo-viewer/PrintModal.tsx"
  - "src/pages/GuestShare.tsx"
autonomous: true
requirements:
  - "SC-03"
  - "PR-01"
must_haves:
  truths:
    - "Unique guest sharing tokens generates public showcase albums mapped to guest emails"
    - "Public /guest/:token page renders chronological photos tab (approved uploads + claims) and guestbook messages tab"
    - "Invalid / guest token route shows a friendly fallback screen"
    - "Print ordering dialog in Lightbox triggers Artifact Uprising and Shutterfly selection modal"
    - "Clicking a provider copies the photo URL to the clipboard with instructions"
    - "All changes conform to standard gold (#d4af37) and cream (#faf8f5) aesthetics"
  artifacts:
    - path: "src/components/photo-viewer/PrintModal.tsx"
      provides: "Glassmorphic print ordering modal with Artifact Uprising and Shutterfly integrations"
    - path: "src/pages/GuestShare.tsx"
      provides: "Public Showcase Album page supporting photo grids and guestbook cursive cards"
  key_links:
    - from: "PhotoLightbox.tsx"
      to: "PrintModal"
      via: "Triggers print modal on Order Prints toolbar click"
    - from: "Upload.tsx"
      to: "supabase.ts"
      via: "Fetches/generates guest share link on upload success"
    - from: "App.tsx"
      to: "GuestShare"
      via: "Registers Dynamic showcase album routes"
---

<objective>
Implement a gorgeous, end-to-end guest showcase sharing experience and high-end print ordering integration. Guests will be able to share their personalized Contribution Album compilation via custom persistent share links, and gallery visitors can order custom matte/canvas prints from professional providers directly inside the photo viewer lightbox.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-shared-links-print/19-CONTEXT.md
@.planning/phases/19-shared-links-print/19-UI-SPEC.md
@src/lib/supabase.ts
@src/components/photo-viewer/PhotoLightbox.tsx
@src/pages/Upload.tsx
@src/App.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Print Ordering Dialog in Lightbox</name>
  <files>src/components/photo-viewer/PrintModal.tsx</files>
  <action>
    Create a new file `src/components/photo-viewer/PrintModal.tsx`:
    1. A premium glassmorphic modal overlay.
    2. Offers two beautifully styled gold-accent cards:
       - Artifact Uprising: "Artisan quality matte prints, brass mounts, and custom wedding albums."
       - Shutterfly: "Affordable classic prints, large canvas wraps, and custom personalized photo books."
    3. Clicking either option opens their respective website in a new window/tab and copies the high-res URL of the current photo to the clipboard.
    4. Provide clear user guidelines: "📋 Direct high-res image link copied! You can paste this URL directly in their photo uploader."
    5. Handle Esc closing, backdrop clicks, and tab-focus traps for complete keyboard accessibility.
  </action>
  <verify>
    Confirm the modal mounts, styles look wowed, and clipboard copying works flawlessly.
  </verify>
  <done>
    PrintModal fully implemented.
  </done>
</task>

<task type="auto">
  <name>Task 2: Integrate Print Trigger in Lightbox</name>
  <files>src/components/photo-viewer/PhotoLightbox.tsx</files>
  <action>
    Integrate print ordering inside the existing `PhotoLightbox.tsx`:
    1. Import `Printer` or `ShoppingBag` icon from `lucide-react`.
    2. Add the "Order Prints" button next to "Download" in the lightbox toolbar, styled matching standard buttons (rounded-full, bg-white/10, hover:bg-white/20, text-white/80).
    3. Mount the new `PrintModal` overlay inside the lightbox, managing open/closed states.
  </action>
  <verify>
    Ensure the button fits nicely on mobile/desktop toolbar layouts and triggers the modal cleanly.
  </verify>
  <done>
    Print trigger integrated in PhotoLightbox.
  </done>
</task>

<task type="auto">
  <name>Task 3: Integrate Share Link Generation on Upload Success</name>
  <files>src/pages/Upload.tsx</files>
  <action>
    Update `/upload` success panel:
    1. Fetch/generate the persistent share token on successful photo upload submission using `getOrCreateShareToken(email)`.
    2. Replace or overlay a premium gold-gradient successful sharing card on the success page.
    3. Display the generated guest contribution showcase link (`/guest/:token`).
    4. Provide a "Copy Link" button featuring an animated checkmark state on click to easily copy the album link.
  </action>
  <verify>
    Run upload flow, verify the share link is populated on success screen and correctly copies.
  </verify>
  <done>
    Upload page share banner completed.
  </done>
</task>

<task type="auto">
  <name>Task 4: Build Public Guest Showcase Page Route `/guest/:token`</name>
  <files>src/pages/GuestShare.tsx</files>
  <action>
    Create a new public showcase page `src/pages/GuestShare.tsx`:
    1. Aesthetic matches the premium site design (gold accents `#d4af37`, cream backgrounds `#faf8f5`, elegant typography).
    2. Shows a gorgeous glassmorphic top card: "[Guest Name]'s Memory Album" with count badges (e.g. "X Photos Uploaded", "Y Messages Written").
    3. Main primary buttons: "Copy Album Link" and "Back to Gallery".
    4. Dual Tab System (Radix-style Tabs):
       - Photos Tab: Masonry flex grid displaying approved uploaded photos and claimed photos. Clicking a photo opens it in our standard `PhotoLightbox` so guests can download, Zoom, like, or Order Prints!
       - Guestbook Tab: Displays their guestbook messages styled as premium handwriting/cursive cards.
    5. Clean expired/invalid token fallback screen when token is invalid, prompting them to check the link or return to gallery.
  </action>
  <verify>
    Verify responsive rendering, lightbox integration, and correct invalid token handling.
  </verify>
  <done>
    GuestShare page fully built.
  </done>
</task>

<task type="auto">
  <name>Task 5: Declare Showcase Route in App.tsx</name>
  <files>src/App.tsx</files>
  <action>
    Add routing for `/guest/:token`:
    1. Lazy load `GuestShare = lazy(() => import('@/pages/GuestShare'))`.
    2. Declare Route `/guest/:token` inside `Routes` wrap, matching our `LazyPage` helper with descriptive title `"Guest Album"`.
  </action>
  <verify>
    Ensure zero compilation errors.
  </verify>
  <done>
    Route declared successfully.
  </done>
</task>

<task type="auto">
  <name>Task 6: Complete QA & Verification</name>
  <files>package.json</files>
  <action>
    Perform full test suite run and production bundle validation:
    1. Run all existing and new unit tests inside the workspace.
    2. Execute `npm run build` to confirm zero TypeScript compilation errors.
  </action>
  <verify>
    Confirm build output compiles clean and tests pass.
  </verify>
  <done>
    Verification complete.
  </done>
</task>

</tasks>

<verification>
Verify complete clean build:
- `npm run build`
Run Vitest:
- `npm run test:run`
</verification>

<success_criteria>
- Seamless public guest showcase album rendered via dynamic token.
- High-end print provider selection modal in Photo Lightbox copies high-res image URL to clipboard.
- Premium UI elements conforming exactly to gold/cream wedding design standards.
- Fully accessible keyboard navigations and screen reader support.
</success_criteria>

<output>
Create `.planning/phases/19-shared-links-print/19-01-SUMMARY.md` on completion.
</output>
