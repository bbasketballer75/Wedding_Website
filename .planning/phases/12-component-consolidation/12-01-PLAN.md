---
phase: 12-component-consolidation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/layout/LoadingSpinner.tsx
autonomous: true
requirements:
  - UX-07
  - UX-08

must_haves:
  truths:
    - "Only one LoadingSpinner implementation exists in the codebase"
    - "DarkModeToggle correctly uses gold tokens for accent states"
  artifacts:
    - path: "src/components/ui/LoadingSpinner.tsx"
      provides: "Canonical LoadingSpinner implementation"
    - path: "src/components/layout/LoadingSpinner.tsx"
      provides: "DELETED - was duplicate"
  key_links: []
---

<objective>
Remove duplicate LoadingSpinner implementation (UX-07) and verify DarkModeToggle gold token usage (UX-08).

Purpose: Eliminate component duplication and confirm correct token usage per user decisions.
Output: Single LoadingSpinner canonical version remains; duplicate removed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@src/components/ui/LoadingSpinner.tsx
@src/components/layout/LoadingSpinner.tsx
@src/components/ui/DarkModeToggle.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove duplicate LoadingSpinner</name>
  <files>src/components/layout/LoadingSpinner.tsx</files>
  <action>
    DELETE the file `src/components/layout/LoadingSpinner.tsx`.

    This file is a duplicate of `src/components/ui/LoadingSpinner.tsx`. Research confirmed zero imports exist for either spinner in the codebase, so no import redirects are needed.

    The canonical version in `src/components/ui/LoadingSpinner.tsx` (165 lines) provides:
    - LoadingSpinner with size ('sm'|'md'|'lg'|'xl') and color ('primary'|'secondary'|'white'|'current') props
    - LoadingOverlay, PageLoading, and LoadingButton exports
    - Framer Motion animation

    The duplicate version in `src/components/layout/LoadingSpinner.tsx` (72 lines) had skeleton variants but is superseded by the canonical version's fuller feature set.

    Do NOT modify `src/components/ui/LazyLoad.tsx` — its inline spinner fallback is intentional per user decision (CONTEXT.md D-UX-07).
  </action>
  <verify>
    <automated>
      # Verify duplicate is deleted
      test ! -f src/components/layout/LoadingSpinner.tsx && echo "DELETED: layout/LoadingSpinner.tsx"

      # Verify canonical still exists
      test -f src/components/ui/LoadingSpinner.tsx && echo "KEPT: ui/LoadingSpinner.tsx"

      # Verify no imports reference the deleted file
      grep -r "layout/LoadingSpinner" src/ --include="*.ts" --include="*.tsx" || echo "CLEAN: No imports of deleted file"
    </automated>
  </verify>
  <done>src/components/layout/LoadingSpinner.tsx deleted; canonical ui/LoadingSpinner.tsx remains</done>
</task>

</tasks>

<verification>
## UX-07 Verification (LoadingSpinner Deduplication)

1. File `src/components/layout/LoadingSpinner.tsx` does not exist
2. File `src/components/ui/LoadingSpinner.tsx` exists and exports LoadingSpinner, LoadingOverlay, PageLoading, LoadingButton
3. No imports reference `layout/LoadingSpinner`

## UX-08 Verification (DarkModeToggle Gold Tokens)

Per research findings (12-RESEARCH.md), DarkModeToggle already correctly implements gold token usage:
- Line 71: `focus:ring-(--color-gold)` for focus ring
- Line 147: `text-(--color-gold)` for active selection indicator

Structural grays (bg-gray-*, text-gray-*, border-gray-*) are appropriate for neutral container elements per user decision (CONTEXT.md D-UX-08).

**No code changes required for UX-08.**
</verification>

<success_criteria>
- [ ] src/components/layout/LoadingSpinner.tsx deleted
- [ ] src/components/ui/LoadingSpinner.tsx preserved
- [ ] No broken imports (verified by grep)
- [ ] UX-08 documented as already satisfied (no action needed)
</success_criteria>

<output>
After completion, create `.planning/phases/12-component-consolidation/12-01-SUMMARY.md`
</output>
