# Plan: Wedding_Website_Clean Major Refactors (2026-09-12)

> **Master plan.** Four subplans; run in dependency order. Each ships as its own PR.

## Subplans

| ID         | Title                                 | Effort    | Depends on                |
| ---------- | ------------------------------------- | --------- | ------------------------- |
| **Plan A** | Gallery.tsx split (2143 → ~300 lines) | 2-3 hours | none                      |
| **Plan B** | TypeScript 5 → 6 → 7 upgrade          | 1 hour    | none                      |
| **Plan C** | ESLint 9 → 10 (closes Dependabot #62) | 30-45 min | Plan B (TS 7) recommended |
| **Plan D** | jsdom 28 → 30 (closes Dependabot #65) | 30-60 min | none                      |

## Execution order (recommended)

```
1. Plan A   (Gallery.tsx split)        — independent, do first
   ↓
2. Plan B   (TypeScript 5→6)           — safe, prerequisite for Plan C
   ↓
3. Plan B'  (TypeScript 6→7)           — unblocks ESLint 10 peer-deps
   ↓
4. Plan C   (ESLint 10)               — closes #62
5. Plan D   (jsdom 30)                 — closes #65  (parallel-OK with C)
```

**Critical path:** A → B → C (3-4 hours total). Plan D can run in parallel with Plan C since jsdom upgrade is independent of eslint.

## Recommended PR sequence

| #   | Branch                              | Title                                | Plan | Estimated time |
| --- | ----------------------------------- | ------------------------------------ | ---- | -------------- |
| 1   | `refactor/gallery-split-2026-09-12` | refactor(gallery): split Gallery.tsx | A    | 2-3h           |
| 2   | `chore/typescript-6`                | chore(deps): bump typescript 5 → 6   | B.1  | 30m            |
| 3   | `chore/typescript-7`                | chore(deps): bump typescript 6 → 7   | B.2  | 30m            |
| 4   | `chore/eslint-10`                   | chore(deps): bump eslint 9 → 10      | C    | 45m            |
| 5   | `chore/jsdom-30`                    | chore(deps): bump jsdom 28 → 30      | D    | 45m            |

## Parallelization strategy

Plans B (TS) and D (jsdom) can run in parallel since they're independent. Plan C requires Plan B to complete first (eslint 10 peer-deps prefer TS 7). Plan A is fully independent.

**Optimal parallel sessions:**

- Session A: Plan A (Gallery split)
- Session B: Plan B.1 → B.2 (TypeScript)
- Session C: Plan D (jsdom)
- (After B done) Session D: Plan C (ESLint 10)

## Verification

Each PR must pass:

- `npm run lint` — 0 errors
- `npx tsc --noEmit` — 0 errors
- `npm run test:run` — all 134 tests pass
- `npm run build` — exit 0
- All 4 GitHub Actions checks pass

## Risks (consolidated)

1. **Plan A** — Pure refactor, low risk. Bundle size regression is the main concern.
2. **Plan B** — Low risk. Major version but verified no API breaks.
3. **Plan C** — Medium risk. ESLint 10 may surface new rule violations requiring small fixes.
4. **Plan D** — Medium risk. jsdom 30 has removed APIs; vitest may need minor version bump.

## Memory + Honcho

After each merged PR, update:

- `~/.hermes/memories/MEMORY.md` (brief entry per PR)
- Honcho conclusion for durable cross-session recall

## Done when

- [ ] All 5 PRs merged
- [ ] Both blocked Dependabot PRs (#62, #65) auto-closed by GitHub
- [ ] All gates green on main HEAD
- [ ] No new dependencies with known CVEs > 0
- [ ] Wedding_Website_Clean is on TypeScript 7, ESLint 10, jsdom 30

**Total estimated effort:** 4-5 hours focused work, mostly serial.

## See also

- `docs/plans/2026-09-12-gallery-split.md`
- `docs/plans/2026-09-12-typescript-upgrade.md`
- `docs/plans/2026-09-12-eslint-10.md`
- `docs/plans/2026-09-12-jsdom-30.md`
