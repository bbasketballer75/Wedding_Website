# Plan C: ESLint 9 → 10 Coordinated Bump

> **For Hermes:** Coordinates the previously-failed Dependabot PRs (#62 and a future eslint bump). Mature unblocks Plan B (TypeScript 7 must land first or in parallel).

**Goal:** Move the project from `eslint@9.39.4` + `@eslint/js@9.39.4` to `eslint@^10.x` + `@eslint/js@^10.0.1`. Closes the previously-failed Dependabot PR #62.

**Architecture:** Single coordinated PR. Pre-flight: Plan B (TypeScript 7) merged first because `@typescript-eslint/utils@8.x` has looser peer-deps with TS 7 than with TS 5.

**Tech Stack:** ESLint 10.x, `@typescript-eslint/*` 8.x (latest minor), `eslint-plugin-react-hooks` 5.x (already ESLint 9 compatible), `eslint-plugin-jsx-a11y` 6.x (latest).

---

## Gate 1: Internal gap analysis

Already done:

- `eslint: 9.39.4` and `@eslint/js: 9.39.4` (in lock-step)
- `eslint-config-prettier` loaded
- `eslint-plugin-react`, `eslint-plugin-react-hooks`, `eslint-plugin-react-refresh`, `eslint-plugin-jsx-a11y` configured
- Custom rules in `eslint.config.js` (flat config)

No eslint v9 → v10 migration scripts in repo.

---

## Gate 2: External artifact pre-flight

ESLint 10.0 release notes (researched):

- **Breaking:** Drops Node 18, requires Node 20+
- **Breaking:** Some legacy formatters removed (irrelevant for this project)
- **Breaking:** Stricter default rule parsing
- `@eslint/js@10` **requires** `eslint@^10` (this is what blocked Dependabot #62)

Node version check: this project requires Node 20.19 (`netlify.toml` shows `NODE_VERSION = "20.19.0"`). **Compatible.**

Compatibility matrix:

- `eslint-plugin-react@7.x` — supports eslint 8/9, ESLint 10 status: ✅ compatible per their matrix
- `eslint-plugin-react-hooks@5.x` — ESLint 9/10 compatible
- `eslint-plugin-react-refresh` — ESLint 9/10 compatible (latest is 0.5.x)
- `eslint-plugin-jsx-a11y@6.x` — ESLint 9/10 compatible
- `@typescript-eslint/*@8.x` — ESLint 9/10 compatible

**Conclusion:** Safe to upgrade as one coordinated PR.

---

## Task breakdown

### Task C.1: Bump eslint + @eslint/js + peer plugins in lockstep

**Files:** `package.json`, `package-lock.json`

**Step 1:** Run `npm install --save-dev eslint@^10.0.0 @eslint/js@^10.0.1 @typescript-eslint/parser@^8.67.0 @typescript-eslint/eslint-plugin@^8.67.0 eslint-plugin-react@^7.37.0 eslint-plugin-react-hooks@^5.0.0 eslint-plugin-react-refresh@^0.4.18 eslint-plugin-jsx-a11y@^6.10.0` — expected: install completes.

**Step 2:** Run `npx tsc --noEmit` — expected: 0 errors.

**Step 3:** Run `npm run lint` — expected: 0 errors (or some new v10 rules — note them, fix in next task).

**Step 4:** If lint has errors: read each, decide if it's a real issue or a rule tightening. Fix real issues; for rule-tightening, add an inline disable with reason or update config to relax that specific rule.

**Step 5:** Run `npm run test:run` — expected: all pass.

**Step 6:** Run `npm run build` — expected: exit 0.

**Step 7:** Commit: `git add package.json package-lock.json eslint.config.js && git commit -m "chore(deps): bump eslint 9 → 10 with plugin coordination

- eslint 9.39.4 → 10.x
- @eslint/js 9.39.4 → 10.0.1
- @typescript-eslint/parser, eslint-plugin: 8.x
- eslint-plugin-react: 7.x
- eslint-plugin-react-hooks: 5.x
- eslint-plugin-react-refresh: 0.4.18
- eslint-plugin-jsx-a11y: 6.x

Closes previously-blocked Dependabot #62."`.

### Task C.2: Open PR and verify CI

**Step 1:** Push branch: `git push -u origin chore/eslint-10`.

**Step 2:** Open PR: `gh pr create --base main --head chore/eslint-10 --title "chore(deps): bump eslint 9 → 10 with coordinated plugins" --body "Major bump. Coordinates the previously-blocked Dependabot #62. Pre-flight verified: ESLint 10 is compatible with Node 20.19 (project's runtime) and all peer plugins. All gates green: lint, tsc, 134 tests, build."`.

**Step 3:** Wait for CI: `gh pr checks <num> --watch` (or poll) — expected: all SUCCESS.

**Step 4:** Merge: `gh pr merge <num> --squash --delete-branch` — expected: merged.

---

## Verification matrix

| Gate                                      | Expected                             | Verify    |
| ----------------------------------------- | ------------------------------------ | --------- |
| `cat package.json \| jq .devDependencies` | eslint `^10.x`, @eslint/js `^10.0.1` | after C.1 |
| `npx eslint --version`                    | `10.x`                               | after C.1 |
| `npm run lint`                            | exit 0, 0 errors                     | after C.1 |
| `npm run test:run`                        | all 134 tests pass                   | after C.1 |
| `npm run build`                           | exit 0                               | after C.1 |
| `gh pr checks <num>`                      | all SUCCESS                          | after C.2 |

---

## Risks

1. **New strict rules** — ESLint 10 may add rules that flag existing code. Mitigation: investigate each, fix real issues.
2. **`@typescript-eslint/utils` peer-dep on TS** — `@typescript-eslint/utils@8.x` peer-deps TS 5/6/7. If Plan B not merged first, TS 5 may need `@typescript-eslint/utils@7.x`. **Order: Plan B before Plan C, or run them in sequence.**
3. **Node version** — Plan C requires Node 20+. Project already requires 20.19 in `netlify.toml`. Safe.

---

## Done when

- [ ] eslint: `^10.x` in package.json
- [ ] @eslint/js: `^10.0.1` in package.json
- [ ] All plugin peer-deps compatible
- [ ] All gates green
- [ ] PR merged
- [ ] Dependabot #62 status: closed (auto-closed by GitHub when superseded)

**Estimated effort:** 30-45 minutes (mostly investigation if new rules fire).
