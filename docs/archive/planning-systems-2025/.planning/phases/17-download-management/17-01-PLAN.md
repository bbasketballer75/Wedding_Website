---
phase: "17"
plan: "01"
type: "execute"
wave: 1
depends_on: []
files_modified:
  - "src/hooks/useLongPress.ts"
  - "src/stores/downloadStore.ts"
  - "src/utils/download.ts"
  - "src/components/gallery/ProgressModal.tsx"
  - "src/components/gallery/DownloadQueuePanel.tsx"
  - "src/pages/Gallery.tsx"
  - "src/components/gallery/PhotoGrid.tsx"
  - "src/components/gallery/VirtualizedPhotoGrid.tsx"
autonomous: true
requirements:
  - "DL-01"
  - "DL-02"
  - "DL-03"
must_haves:
  truths:
    - "User can long-press (500ms) on mobile to trigger multi-select mode"
    - "Selected count displays in the header during multi-select mode"
    - "DownloadQueuePanel FAB displays interactive gold pill badge with current queue count"
    - "DownloadQueuePanel drawer shows queued thumbnails with Remove options and a destructive clear action"
    - "Download CTA triggers the hybrid client/server zip generator downloadBatch"
    - "Queue persists safely across page reloads in sessionStorage"
  artifacts:
    - path: "src/hooks/useLongPress.ts"
      provides: "Long-press hook with scroll-cancellation support"
    - path: "src/stores/downloadStore.ts"
      provides: "Zustand queue store with sessionStorage persistence and 50 hard cap"
    - path: "src/utils/download.ts"
      provides: "Hybrid batch zipping engine (JSZip client-side <=20, Server-side >20)"
    - path: "src/components/gallery/DownloadQueuePanel.tsx"
      provides: "FAB drawer panel for queue management"
    - path: "src/components/gallery/ProgressModal.tsx"
      provides: "Gold HSL accented full-screen progress modal"
  key_links:
    - from: "PhotoGridItem & VirtualizedPhotoItem"
      to: "useLongPress hook"
      via: "Touch-press hook bindings"
    - from: "Gallery.tsx"
      to: "downloadStore"
      via: "State mapping and useMemo derived selectors"
---

<objective>
Implement a robust multi-select batch download queue for the wedding gallery web application. This encompasses custom touch long-press hooks, a persistent Zustand queue store with a strict 50-photo hard cap, a hybrid client/server zipping utility to prevent tab memory crash, beautiful gold HSL accented frontend widgets, and deep page integration.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-download-management/17-CONTEXT.md
@src/pages/Gallery.tsx
@src/components/gallery/PhotoGrid.tsx
@src/components/gallery/VirtualizedPhotoGrid.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write src/hooks/useLongPress.ts hook</name>
  <files>src/hooks/useLongPress.ts, src/hooks/useLongPress.test.ts</files>
  <action>
    Create the `useLongPress` hook supporting both touch and mouse duration triggers:
    1. Fire callback if touch/mouse remains down for 500ms.
    2. Exclude right-clicks (button === 2).
    3. Include scroll-cancellation (cancel if touch movement exceeds 10px threshold) to prevent selection while swiping down grids.
    4. Implement full test suite covering click fallback, long press timeout, and movement thresholds.
  </action>
  <verify>
    Run vitest on the new hook test suite.
  </verify>
  <acceptance_criteria>
    - useLongPress.ts exports useLongPress hook.
    - useLongPress.test.ts verifies press duration, right-clicks, and drag cancels.
  </acceptance_criteria>
  <done>
    useLongPress hook implemented and verified via unit tests.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write src/stores/downloadStore.ts store</name>
  <files>src/stores/downloadStore.ts, src/stores/downloadStore.test.ts</files>
  <action>
    Implement Zustand download management store:
    1. Define queue state consisting of queued photo objects.
    2. Configure persist middleware using a safe sessionStorage wrapper to handle quota limit checks.
    3. Impose a strict queue limit hard cap of 50 items.
    4. Add detailed unit tests for add, remove, clear, and persistence limits.
  </action>
  <verify>
    Run vitest on download store tests.
  </verify>
  <acceptance_criteria>
    - downloadStore exports useDownloadStore hook.
    - Limits items correctly to 50 max.
    - Unit tests pass.
  </acceptance_criteria>
  <done>
    Zustand downloadStore configured and unit tested.
  </done>
</task>

<task type="auto">
  <name>Task 3: Write hybrid batch download engine in src/utils/download.ts</name>
  <files>src/utils/download.ts, src/utils/download.test.ts</files>
  <action>
    Build a unified batch download utility:
    1. Small Batches (<= 20 photos): Fetch parallel blobs, pack on client via JSZip, and provide percent-progress reporting.
    2. Large Batches (21 - 50 photos): POST list to server function to streamline a zip payload, avoiding client-side crashes.
    3. Build corresponding unit tests mocking zip packing and endpoint calls.
  </action>
  <verify>
    Run vitest on download utility tests.
  </verify>
  <acceptance_criteria>
    - downloadBatch utility correctly routes small vs large zips.
    - Progress callback triggers regularly during small batch processing.
  </acceptance_criteria>
  <done>
    Hybrid zip compilation utility implemented and verified.
  </done>
</task>

<task type="auto">
  <name>Task 4: Build frontend overlays and widgets</name>
  <files>src/components/gallery/ProgressModal.tsx, src/components/gallery/DownloadQueuePanel.tsx</files>
  <action>
    Build UI widgets matching brand standards:
    1. Create ProgressModal: A full-screen glassmorphic overlay containing a gold HSL (#d4af37) loading bar and status descriptors.
    2. Create DownloadQueuePanel: Bottom-right FAB displaying badge count, expanding into a gorgeous slide-up drawer containing thumbnails, explicit remove CTAs, destructive clear prompts, and a download action trigger.
  </action>
  <verify>
    Confirm visual imports compile cleanly without TypeScript issues.
  </verify>
  <acceptance_criteria>
    - ProgressModal and DownloadQueuePanel compile cleanly.
    - Appropriate accessibility properties (aria-label, aria-pressed, gold focus rings) are defined.
  </acceptance_criteria>
  <done>
    Frontend queue panels and download progress overlays built.
  </done>
</task>

<task type="auto">
  <name>Task 5: Integrate components into the main gallery page</name>
  <files>src/pages/Gallery.tsx, src/components/gallery/PhotoGrid.tsx, src/components/gallery/VirtualizedPhotoGrid.tsx</files>
  <read_first>src/pages/Gallery.tsx, src/components/gallery/PhotoGrid.tsx, src/components/gallery/VirtualizedPhotoGrid.tsx</read_first>
  <action>
    Wire everything into the gallery layout:
    1. Extract item rendering callbacks into standalone sub-components (PhotoGridItem and VirtualizedPhotoItem) in PhotoGrid.tsx and VirtualizedPhotoGrid.tsx.
    2. Bind the useLongPress hooks inside these new grid sub-components to safely avoid React hook-in-loop violations.
    3. In Gallery.tsx, map the selected ids directly to the Zustand store using derived useMemo selectors.
    4. Support action triggers "Select All Visible" and "Clear Selection" inside the controls bar marked with data-testid="gallery-control-bar".
    5. Wire a share URL parser in Gallery.tsx to pre-populate the queue if url queries like /gallery?share=id,id are accessed.
    6. Mount the DownloadQueuePanel and ProgressModal at the root of Gallery.tsx.
  </action>
  <verify>
    Verify compile and TypeScript build.
  </verify>
  <acceptance_criteria>
    - Standalone grid sub-components exist and contain long-press handlers.
    - Derived selection state successfully updates from store selectors.
    - Control bar includes Select All / Clear triggers in multi-select mode.
  </acceptance_criteria>
  <done>
    Grid items and page layout integrated with queue actions.
  </done>
</task>

<task type="auto">
  <name>Task 6: Complete QA validations</name>
  <files>package.json</files>
  <action>
    Validate build stability and run all test modules:
    1. Run all unit and integration tests using vitest.
    2. Verify overall site responsiveness and zero regressions.
  </action>
  <verify>
    npm run test:run
  </verify>
  <acceptance_criteria>
    - 100% of the Vitest test suite passes cleanly.
  </acceptance_criteria>
  <done>
    All QA validations completed successfully.
  </done>
</task>

</tasks>

<verification>
Run the automated test runner to ensure all test suites pass without error:
- `npm run test:run`
Verify build succeeds with zero errors:
- `npm run build`
</verification>

<success_criteria>
- All grid touch gestures and selection states fully operational.
- Queue store perfectly persists in sessionStorage.
- Download compilation automatically routes via hybrid JSZip client/server stream.
- Zero hook-in-loop violations or TypeScript compiler issues.
- 100% Vitest test suite success rate.
</success_criteria>

<output>
After completion, create `.planning/phases/17-download-management/17-01-SUMMARY.md`
</output>
