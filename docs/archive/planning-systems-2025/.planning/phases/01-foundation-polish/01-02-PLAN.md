---
phase: "01-foundation-polish"
plan: "02"
type: "execute"
wave: 1
depends_on: []
files_modified:
  - "src/pages/admin/PhotoModeration.tsx"
  - "src/pages/admin/Dashboard.tsx"
  - "src/pages/admin/GuestbookModeration.tsx"
  - "src/pages/admin/AdminLayout.tsx"
autonomous: true
requirements:
  - "POLISH-01"
  - "POLISH-02"
  - "ADMIN-01"

must_haves:
  truths:
    - "Admin pages show graceful error UI (not white screens) when components fail"
  artifacts:
    - path: "src/pages/admin/PhotoModeration.tsx"
      provides: "ComponentErrorBoundary wrapper for PhotoModeration"
      min_lines: 3
    - path: "src/pages/admin/Dashboard.tsx"
      provides: "ComponentErrorBoundary wrapper for Dashboard"
      min_lines: 3
    - path: "src/pages/admin/GuestbookModeration.tsx"
      provides: "ComponentErrorBoundary wrapper for GuestbookModeration"
      min_lines: 3
  key_links:
    - from: "src/pages/admin/PhotoModeration.tsx"
      to: "@/components/error/ErrorBoundary"
      via: "import { ComponentErrorBoundary }"
      pattern: "import.*ComponentErrorBoundary"
    - from: "src/pages/admin/Dashboard.tsx"
      to: "@/components/error/ErrorBoundary"
      via: "import { ComponentErrorBoundary }"
      pattern: "import.*ComponentErrorBoundary"
    - from: "src/pages/admin/GuestbookModeration.tsx"
      to: "@/components/error/ErrorBoundary"
      via: "import { ComponentErrorBoundary }"
      pattern: "import.*ComponentErrorBoundary"
---

<objective>
Deploy ComponentErrorBoundary on all admin page components to prevent white screens. Wrap PhotoModeration, Dashboard, GuestbookModeration with friendly error UI + retry button.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/components/error/ErrorBoundary.tsx
@src/pages/admin/AdminLayout.tsx
@src/pages/admin/PhotoModeration.tsx
@src/pages/admin/Dashboard.tsx
@src/components/admin/AlbumOrganizer.tsx (analog - already has ComponentErrorBoundary at line 754)
@.planning/phases/01-foundation-polish/01-CONTEXT.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/components/error/ErrorBoundary.tsx (lines 133-157):
```typescript
export const ComponentErrorBoundary: React.FC<{ children: ReactNode; componentName?: string }> = ({
  children,
  componentName,
}) => (
  <ErrorBoundary
    fallback={
      <div className='p-8 text-center bg-gray-50 dark:bg-gray-800 rounded-xl border border-gray-200 dark:border-gray-700'>
        <h3 className='text-lg font-semibold text-gray-900 dark:text-white mb-2'>
          {componentName || 'Component'} Failed to Load
        </h3>
        <p className='text-sm text-gray-600 dark:text-gray-400 mb-4'>
          Something went wrong while rendering this section.
        </p>
        <button
          onClick={() => window.location.reload()}
          className='text-gold hover:underline text-sm font-medium'
        >
          Refresh Page
        </button>
      </div>
    }
  >
    {children}
  </ErrorBoundary>
)
```

From src/components/admin/AlbumOrganizer.tsx (lines 754, 1022):
```typescript
// Already has ComponentErrorBoundary wrapping the component
<ComponentErrorBoundary componentName="Album Organizer">
  {/* component content */}
</ComponentErrorBoundary>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wrap Dashboard with ComponentErrorBoundary</name>
  <files>src/pages/admin/Dashboard.tsx</files>
  <read_first>src/pages/admin/Dashboard.tsx</read_first>
  <action>
Wrap the Dashboard component export with ComponentErrorBoundary.

1. Add import at top of file (after existing imports):
```typescript
import { ComponentErrorBoundary } from '@/components/error/ErrorBoundary'
```

2. Find the Dashboard component export (line 115):
```typescript
export function Dashboard() {
```

Wrap the entire component JSX return with ComponentErrorBoundary:

```typescript
export function Dashboard() {
  return (
    <ComponentErrorBoundary componentName="Dashboard">
      {/* existing JSX */}
    </ComponentErrorBoundary>
  )
}
```

Note: The component uses lazy loading in AdminLayout.tsx routes, so the error boundary wraps the component definition, not the route.
</action>
  <verify>
<automated>grep -n "ComponentErrorBoundary" src/pages/admin/Dashboard.tsx</automated>
  </verify>
  <done>
Dashboard.tsx has ComponentErrorBoundary with componentName="Dashboard" wrapping the component
</done>
</task>

<task type="auto">
  <name>Task 2: Wrap PhotoModeration with ComponentErrorBoundary</name>
  <files>src/pages/admin/PhotoModeration.tsx</files>
  <read_first>src/pages/admin/PhotoModeration.tsx</read_first>
  <action>
Wrap the PhotoModeration component export with ComponentErrorBoundary.

1. Add import at top of file (check if not already present):
```typescript
import { ComponentErrorBoundary } from '@/components/error/ErrorBoundary'
```

2. Find the PhotoModeration component export (line 87):
```typescript
export function PhotoModeration() {
```

Wrap the entire component JSX return with ComponentErrorBoundary:

```typescript
export function PhotoModeration() {
  return (
    <ComponentErrorBoundary componentName="Photo Moderation">
      {/* existing JSX */}
    </ComponentErrorBoundary>
  )
}
```
</action>
  <verify>
<automated>grep -n "ComponentErrorBoundary" src/pages/admin/PhotoModeration.tsx</automated>
  </verify>
  <done>
PhotoModeration.tsx has ComponentErrorBoundary with componentName="Photo Moderation" wrapping the component
</done>
</task>

<task type="auto">
  <name>Task 3: Wrap GuestbookModeration with ComponentErrorBoundary</name>
  <files>src/pages/admin/GuestbookModeration.tsx</files>
  <read_first>src/pages/admin/GuestbookModeration.tsx</read_first>
  <action>
Wrap the GuestbookModeration component export with ComponentErrorBoundary.

1. Add import at top of file:
```typescript
import { ComponentErrorBoundary } from '@/components/error/ErrorBoundary'
```

2. Find the GuestbookModeration component export:
```typescript
export function GuestbookModeration() {
```

Wrap the entire component JSX return with ComponentErrorBoundary:

```typescript
export function GuestbookModeration() {
  return (
    <ComponentErrorBoundary componentName="Guestbook Moderation">
      {/* existing JSX */}
    </ComponentErrorBoundary>
  )
}
```
</action>
  <verify>
<automated>grep -n "ComponentErrorBoundary" src/pages/admin/GuestbookModeration.tsx</automated>
  </verify>
  <done>
GuestbookModeration.tsx has ComponentErrorBoundary with componentName="Guestbook Moderation" wrapping the component
</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → React | Error boundary catches component render failures |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | Denial of Service | admin pages | mitigate | ComponentErrorBoundary shows friendly UI + refresh button instead of white screen |
</threat_model>

<verification>
Run `npm run lint -- --fix` to ensure imports are correct. Verify all three components have ComponentErrorBoundary applied.
</verification>

<success_criteria>
- [ ] Dashboard.tsx has ComponentErrorBoundary with componentName="Dashboard"
- [ ] PhotoModeration.tsx has ComponentErrorBoundary with componentName="Photo Moderation"  
- [ ] GuestbookModeration.tsx has ComponentErrorBoundary with componentName="Guestbook Moderation"
- [ ] All three components use same pattern as AlbumOrganizer.tsx
</success_criteria>

<output>
After completion, create .planning/phases/01-foundation-polish/01-02-SUMMARY.md
</output>