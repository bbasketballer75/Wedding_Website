---
phase: "01-foundation-polish"
plan: "03"
type: "execute"
wave: 2
depends_on: []
files_modified:
  - "vite.config.js"
  - "src/components/photo-viewer/PhotoLightbox.tsx"
  - "src/App.tsx"
  - "src/components/layout/Header.tsx"
autonomous: true
requirements:
  - "POLISH-03"
  - "POLISH-04"
  - "POLISH-05"
  - "POLISH-06"

must_haves:
  truths:
    - "Lightbox responds to arrow keys (prev/next) and ESC to close, with visible close button"
    - "All console.log/warn/error replaced with logger utility in production"
    - "Page transitions use consistent Framer Motion animations across all routes"
    - "Hamburger menu works on all pages with smooth open/close transitions"
  artifacts:
    - path: "vite.config.js"
      provides: "esbuild drop console configured for production"
      contains: "drop: mode === 'production'"
    - path: "src/components/photo-viewer/PhotoLightbox.tsx"
      provides: "Keyboard handler for Escape, ArrowLeft, ArrowRight"
      min_lines: 15
    - path: "src/App.tsx"
      provides: "PageTransition wrapper on all LazyPage components"
      min_lines: 10
  key_links:
    - from: "src/components/photo-viewer/PhotoLightbox.tsx"
      to: "document"
      via: "addEventListener('keydown', handleKeyDown)"
      pattern: "addEventListener.*keydown"
    - from: "src/App.tsx"
      to: "LazyPage"
      via: "PageTransition wrapper"
      pattern: "PageTransition.*children"
---

<objective>
Verify and confirm existing implementations of lightbox keyboard nav, console.* removal, page transitions, and mobile navigation work correctly per spec.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@vite.config.js
@src/components/photo-viewer/PhotoLightbox.tsx
@src/App.tsx
@src/components/layout/Header.tsx
@.planning/phases/01-foundation-polish/01-RESEARCH.md
@.planning/phases/01-foundation-polish/01-PATTERNS.md

<interfaces>
<!-- Key patterns from codebase -->

From vite.config.js (line 242):
```javascript
esbuild: {
  drop: mode === 'production' ? ['console', 'debugger'] : [],
},
```
This is ALREADY CONFIGURED. Verification needed.

From PhotoLightbox.tsx (lines 137-149):
```typescript
const handleKeyDown = (e: KeyboardEvent) => {
  if (e.key === 'Escape') { onClose(); return }
  if (e.key === 'ArrowLeft') { handlePrevious(); return }
  if (e.key === 'ArrowRight') { handleNext(); return }
  if (e.key === 'l') { currentPhoto && onLike?.(currentPhoto.id) }
}
document.addEventListener('keydown', handleKeyDown)
```
This is ALREADY IMPLEMENTED. Verification needed.

From App.tsx (lines 26-37):
```typescript
function PageTransition({ children }) {
  return (
    <motion.div
      initial={{ opacity: 0, y: 20 }}
      animate={{ opacity: 1, y: 0 }}
      exit={{ opacity: 0, y: -20 }}
      transition={{ duration: 0.5, ease: [0.16, 1, 0.3, 1] }}
    >
      {children}
    </motion.div>
  )
}
```
This is ALREADY WORKING. Verification needed.

From Header.tsx: Mobile hamburger menu behavior - need to verify it exists and works.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify esbuild console drop is working</name>
  <files>src/App.tsx</files>
  <read_first>vite.config.js</read_first>
  <action>
Run production build and verify no console.* calls remain:

```bash
npm run build
```

Then check the output bundle for console calls:
```bash
grep -r "console\." dist/assets/*.js | wc -l
```

Expected: 0 (zero console.* calls in production bundle)

If count is > 0, investigate which files still have console.* and fix them. The esbuild drop is configured but may not catch dynamic console calls or calls in certain patterns.

Note: Development builds will retain console.* - this is expected. Only production builds should be stripped.
</action>
  <verify>
<automated>npm run build 2>&1 | tail -20 && grep -r "console\." dist/assets/*.js 2>/dev/null | wc -l</automated>
  </verify>
  <done>
Production build succeeds with 0 console.* calls in dist/assets/*.js
</done>
</task>

<task type="auto">
  <name>Task 2: Verify PhotoLightbox keyboard navigation</name>
  <files>src/components/photo-viewer/PhotoLightbox.tsx</files>
  <read_first>src/components/photo-viewer/PhotoLightbox.tsx</read_first>
  <action>
Read PhotoLightbox.tsx lines 131-150 to confirm keyboard handler:

1. Verify handleKeyDown function handles:
   - 'Escape' → calls onClose()
   - 'ArrowLeft' → calls handlePrevious()
   - 'ArrowRight' → calls handleNext()

2. Verify the event listener is attached to document:
   ```typescript
   document.addEventListener('keydown', handleKeyDown)
   ```

3. Check for visible close button in the lightbox UI (search for X button or close button implementation)

4. If any of these are missing or incomplete, fix them according to D-05:
   - Arrow keys to navigate prev/next
   - ESC to close
   - Visible close button

The keyboard handler should be in a useEffect that runs when isOpen changes.
</action>
  <verify>
<automated>grep -n "ArrowLeft\|ArrowRight\|Escape\|onClose" src/components/photo-viewer/PhotoLightbox.tsx | head -20</automated>
  </verify>
  <done>
PhotoLightbox has keyboard handler with ArrowLeft/ArrowRight/Escape, and visible close button in UI
</done>
</task>

<task type="auto">
  <name>Task 3: Verify PageTransition on all routes</name>
  <files>src/App.tsx</files>
  <read_first>src/App.tsx</read_first>
  <action>
Read App.tsx and verify:

1. PageTransition function exists (around line 26-37) with correct Framer Motion animation:
   - initial: opacity 0, y 20
   - animate: opacity 1, y 0
   - exit: opacity 0, y -20
   - transition duration 0.5

2. LazyPage component wraps children with PageTransition (line 40-56)

3. All route definitions use LazyPage wrapper (lines 116-208)

4. Confirm no route bypasses PageTransition

If PageTransition is missing on any route, add the wrapper.
</action>
  <verify>
<automated>grep -n "PageTransition" src/App.tsx && grep -c "LazyPage" src/App.tsx</automated>
  </verify>
  <done>
PageTransition wraps all LazyPage components in App.tsx routes
</done>
</task>

<task type="auto">
  <name>Task 4: Verify mobile hamburger menu functionality</name>
  <files>src/components/layout/Header.tsx</files>
  <read_first>src/components/layout/Header.tsx</read_first>
  <action>
Read Header.tsx fully and verify:

1. Mobile menu (hamburger) exists - look for menu toggle button that shows on small screens

2. Menu opens/closes smoothly with Framer Motion animations

3. Menu works on ALL pages (not just home page)

4. Check for AnimatePresence usage on mobile menu

5. Verify the menu state is managed correctly and doesn't conflict with route changes

If hamburger menu is missing or incomplete, implement according to D-07:
- Hamburger icon button that appears on mobile
- Smooth open/close transitions using Framer Motion
- Menu works consistently across all public pages

Note: The Header is shown on all pages except home (/, Admin routes). Verify mobile menu works on gallery, film, upload, guestbook, people pages.
</action>
  <verify>
<automated>grep -n "hamburger\|menu\|mobile\|AnimatePresence" src/components/layout/Header.tsx</automated>
  </verify>
  <done>
Mobile hamburger menu exists with smooth transitions, works on all pages
</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser | User presses keyboard keys in lightbox |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Denial of Service | PhotoLightbox | accept | Missing keyboard nav is usability issue, not security |
| T-01-05 | Information Disclosure | Console logs | mitigate | esbuild drop removes console from production |
</threat_model>

<verification>
Run `npm run lint -- --fix` to check for any issues. Review that all four POLISH requirements are satisfied.
</verification>

<success_criteria>
- [ ] Production build has 0 console.* calls (esbuild drop working)
- [ ] PhotoLightbox handles ArrowLeft/ArrowRight/Escape keys + has visible close button
- [ ] All routes in App.tsx wrapped with PageTransition
- [ ] Mobile hamburger menu works on all public pages with smooth transitions
</success_criteria>

<output>
After completion, create .planning/phases/01-foundation-polish/01-03-SUMMARY.md
</output>