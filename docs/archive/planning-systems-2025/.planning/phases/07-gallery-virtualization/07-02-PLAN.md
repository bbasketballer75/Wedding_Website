---
phase: 07-gallery-virtualization
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - src/components/photo-viewer/PhotoLightbox.tsx
  - src/pages/Gallery.tsx
autonomous: false
requirements:
  - GAL-01

must_haves:
  truths:
    - "Lightbox opens for any virtualized photo without issues"
    - "Lightbox renders via React Portal to document.body (scroll preservation)"
    - "Prefetch for lightbox navigation works (±5 photos, 11 total)"
  artifacts:
    - path: "src/components/photo-viewer/PhotoLightbox.tsx"
      provides: "Lightbox with Portal to body for scroll preservation"
      contains: "createPortal"
    - path: "src/pages/Gallery.tsx"
      provides: "Gallery page using VirtualizedPhotoGrid"
      replaces: "PhotoGrid with VirtualizedPhotoGrid"
  key_links:
    - from: "src/pages/Gallery.tsx"
      to: "src/components/gallery/VirtualizedPhotoGrid.tsx"
      via: "import and PhotoGrid replacement"
      pattern: "VirtualizedPhotoGrid"
    - from: "src/components/photo-viewer/PhotoLightbox.tsx"
      to: "document.body"
      via: "React Portal"
      pattern: "createPortal"
---

<objective>
Integrate lightbox with portal rendering and replace PhotoGrid with VirtualizedPhotoGrid in Gallery.tsx. Lightbox must render via React Portal to document.body, completely outside virtualized scroll container, preserving scroll position.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-gallery-virtualization/07-CONTEXT.md
@src/components/photo-viewer/PhotoLightbox.tsx
@src/pages/Gallery.tsx
@src/components/gallery/VirtualizedPhotoGrid.tsx
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>VirtualizedPhotoGrid component with row-based @tanstack/react-virtual masonry layout</what-built>
  <how-to-verify>
    1. Run: npm run dev
    2. Navigate to http://localhost:5173/gallery
    3. Select a collection with many photos (e.g., "Wedding Photos")
    4. Open DevTools Performance tab, check "Memory" checkbox
    5. Scroll through the gallery rapidly
    6. Verify:
       - Gallery scrolls smoothly without lag/jank
       - Only visible rows are rendered in DOM (check Elements panel - only ~10-20 photo elements should exist)
       - Photo hover animations work (scale, shadow)
       - PhotoLikeButton appears on hover
       - Select mode works (if enabled)
    7. Run: npm run lint -- --quiet
    8. Verify: No ESLint errors
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Integrate lightbox Portal and connect to VirtualizedPhotoGrid</name>
  <files>src/components/photo-viewer/PhotoLightbox.tsx, src/pages/Gallery.tsx</files>
  <read_first>
    src/components/photo-viewer/PhotoLightbox.tsx
    src/pages/Gallery.tsx
  </read_first>
  <action>
    Step A: Modify PhotoLightbox.tsx to use createPortal for Portal rendering.

    Per D-03: "Lightbox renders via React Portal to document.body, completely outside the virtualized scroll container. Gallery scroll position is preserved behind the overlay."

    Current PhotoLightbox.tsx renders directly in the component tree. Change to:
    ```typescript
    import { createPortal } from 'react-dom'

    // At the render location where the lightbox overlay is returned:
    // Replace direct return with portal to document.body
    return createPortal(
      <motion.div ...> {/* existing lightbox JSX */} </motion.div>,
      document.body
    )
    ```

    Keep existing Framer Motion AnimatePresence wrapping — portal should still animate in/out properly.

    Step B: Replace PhotoGrid with VirtualizedPhotoGrid in Gallery.tsx.

    In Gallery.tsx:
    - Import VirtualizedPhotoGrid from @/components/gallery/VirtualizedPhotoGrid
    - Find where PhotoGrid is used (around line 1320-1339 where viewMode is masonry or grid)
    - Replace PhotoGrid with VirtualizedPhotoGrid, passing same props:
      - photos={displayedItems}
      - onPhotoClick={selectMode ? undefined : (_, index) => openLightbox(index)}
      - onLike={selectMode ? undefined : handleLike}
      - selectMode={selectMode}
      - selectedIds={selectedPhotoIds}
      - onToggleSelect={handleToggleSelect}

    Note: VirtualizedPhotoGrid handles masonry and grid views internally. The viewMode prop should still work - VirtualizedPhotoGrid internally uses masonry (VirtualizedMasonryGrid) or standard layout based on viewMode.

    Verify the lightbox still works:
    - PhotoLightbox receives filteredPhotos from Gallery.tsx
    - Lightbox navigation (prev/next) uses useGalleryStore state (selectedImageIndex)
    - openLightbox sets selectedImageIndex via useGalleryStore.getState().openImageModal(index)
  </action>
  <verify>
    <automated>grep -q "createPortal" src/components/photo-viewer/PhotoLightbox.tsx && grep -q "VirtualizedPhotoGrid" src/pages/Gallery.tsx && echo "LIGHTBOX_PORTAL_AND_GALLERY_INTEGRATION_OK"</automated>
  </verify>
  <done>PhotoLightbox renders via React Portal to document.body; Gallery.tsx uses VirtualizedPhotoGrid; lightbox opens/closes correctly for virtualized photos</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify lightbox + virtualization integration</name>
  <what-built>PhotoLightbox with Portal rendering and VirtualizedPhotoGrid integration in Gallery.tsx</what-built>
  <how-to-verify>
    1. Run: npm run dev
    2. Navigate to http://localhost:5173/gallery
    3. Select "Wedding Photos" collection (likely has most photos)
    4. Click on any photo to open lightbox
    5. Verify:
       - Lightbox opens smoothly (overlay covers the page)
       - Lightbox renders via React Portal (check DOM - should be direct child of body, not inside scroll container)
       - Photo displays correctly with caption, faces, etc.
       - Prev/Next navigation works (arrows and keyboard)
       - ESC to close works
       - Scroll position of gallery is preserved behind lightbox
    6. Navigate to multiple photos via lightbox, verify:
       - Prefetching indicator (adjacent photos load smoothly)
       - No flicker or loading issues on navigation
    7. Check Performance: With 200+ photos, scroll performance should remain smooth
    8. Run: npm run lint -- --quiet
    9. Verify: No ESLint errors
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- createPortal used in PhotoLightbox.tsx
- PhotoLightbox renders to document.body (not inside virtualized container)
- VirtualizedPhotoGrid imported and used in Gallery.tsx
- Gallery page scrolls smoothly with virtualization active
- Lightbox opens for any photo in the virtualized grid
- Lightbox navigation works with prefetching
- Scroll position preserved when lightbox opens
</verification>

<success_criteria>
1. PhotoLightbox renders via createPortal to document.body
2. Gallery uses VirtualizedPhotoGrid (replaces PhotoGrid)
3. Lightbox opens/closes for any virtualized photo
4. Prev/next navigation works correctly
5. Gallery scroll position preserved behind lightbox overlay
6. Prefetching works (±5 photos, 11 total)
</success_criteria>

<output>
After completion, create `.planning/phases/07-gallery-virtualization/07-02-SUMMARY.md`
</output>