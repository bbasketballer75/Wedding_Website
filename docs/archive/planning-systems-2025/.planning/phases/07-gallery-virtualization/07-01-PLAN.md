---
phase: 07-gallery-virtualization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/gallery/VirtualizedPhotoGrid.tsx
  - src/components/gallery/components/VirtualizedMasonryGrid.tsx
  - src/components/gallery/PhotoGrid.tsx
autonomous: false
requirements:
  - GAL-01

must_haves:
  truths:
    - "Gallery page scrolls smoothly with 200+ photos (no lag/jank)"
    - "Only visible photos are rendered in DOM (virtualization working)"
    - "Masonry layout preserved with row-based virtualization approach"
    - "Lightbox opens for any virtualized photo without issues"
    - "Adjacent photos prefetch for smooth lightbox navigation"
  artifacts:
    - path: "src/components/gallery/VirtualizedPhotoGrid.tsx"
      provides: "Virtualized photo grid using @tanstack/react-virtual"
      min_lines: 150
    - path: "src/components/gallery/components/VirtualizedMasonryGrid.tsx"
      provides: "Row-based masonry layout with ResizeObserver measurement"
      exports: "useVirtualizer, count, getScrollElement, estimateSize"
  key_links:
    - from: "src/pages/Gallery.tsx"
      to: "src/components/gallery/VirtualizedPhotoGrid.tsx"
      via: "import and PhotoGrid replacement"
      pattern: "VirtualizedPhotoGrid"
    - from: "src/components/gallery/VirtualizedPhotoGrid.tsx"
      to: "@tanstack/react-virtual"
      via: "useVirtualizer hook"
      pattern: "useVirtualizer"
    - from: "src/components/gallery/VirtualizedMasonryGrid.tsx"
      to: "src/stores/galleryStore.ts"
      via: "scroll position and lightbox state"
      pattern: "useGalleryStore"
---

<objective>
Implement gallery virtualization using @tanstack/react-virtual with row-based masonry approach. Replace current column-fill masonry with row-based layout that groups photos into visual rows filling viewport width. Lightbox renders via React Portal outside the virtualized scroll container.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-gallery-virtualization/07-CONTEXT.md
@.planning/phases/06-guest-reactions-upload-status/06-CONTEXT.md

# Library docs
@tanstack/react-virtual documentation (Context7 or npm)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install @tanstack/react-virtual</name>
  <files>package.json</files>
  <read_first>
    package.json
  </read_first>
  <action>
    Run: npm install @tanstack/react-virtual

    Verify installation by checking package.json dependencies includes "@tanstack/react-virtual" and node_modules/@tanstack/react-virtual exists.
  </action>
  <verify>
    <automated>grep -q '"@tanstack/react-virtual"' package.json && ls node_modules/@tanstack/react-virtual 2>/dev/null && echo "INSTALLED"</automated>
  </verify>
  <done>@tanstack/react-virtual installed and importable</done>
</task>

<task type="auto">
  <name>Task 2: Create VirtualizedMasonryGrid component</name>
  <files>src/components/gallery/components/VirtualizedMasonryGrid.tsx</files>
  <read_first>
    src/components/gallery/components/MasonryGrid.tsx
    src/components/gallery/PhotoGrid.tsx
  </read_first>
  <action>
    Create VirtualizedMasonryGrid.tsx implementing row-based masonry using @tanstack/react-virtual.

    Key implementation per D-01, D-02, D-06:
    - Use useVirtualizer with count=estimatedRows, getScrollElement=gallery scroll container ref, estimateSize=averagePhotoHeight + gap
    - hasFixedSize=false (per D-02)
    - Group photos into visual rows: calculate photosPerRow based on current breakpoint columns (base:1, sm:2, md:3, lg:4 per D-05)
    - For each row: calculate which photos fit based on aspect ratios and target row height (~280px per D-01)
    - Use ResizeObserver on each row container to measure actual rendered height (per D-02)
    - Store measured heights in a Map<number, number> for virtualization override
    - Each row is a virtualized item; photos within rows use flex layout with consistent row height
    - Export useVirtualizer, rowCount, getRowMeasurements

    Column breakpoints (per D-05):
    ```typescript
    const MASONRY_COLUMNS = { base: 1, sm: 2, md: 3, lg: 4 } as const
    ```

    Row calculation logic:
    - Get current column count from breakpoint (use window.innerWidth)
    - Group photos: start new row when column count reached OR cumulative aspect ratios exceed row target width
    - Row height: target ~280px, photos flex to fill height while maintaining aspect ratio within the row

    Import useVirtualizer:
    ```typescript
    import { useVirtualizer } from '@tanstack/react-virtual'
    ```
  </action>
  <verify>
    <automated>grep -q "useVirtualizer" src/components/gallery/components/VirtualizedMasonryGrid.tsx && grep -q "MASONRY_COLUMNS" src/components/gallery/components/VirtualizedMasonryGrid.tsx && echo "VIRTUALIZED_MASONRY_GRID_OK"</automated>
  </verify>
  <done>VirtualizedMasonryGrid component created with row-based layout, ResizeObserver measurement, and useVirtualizer integration</done>
</task>

<task type="auto">
  <name>Task 3: Create VirtualizedPhotoGrid component</name>
  <files>src/components/gallery/VirtualizedPhotoGrid.tsx</files>
  <read_first>
    src/components/gallery/PhotoGrid.tsx
    src/components/gallery/components/VirtualizedMasonryGrid.tsx
    src/stores/galleryStore.ts
  </read_first>
  <action>
    Create VirtualizedPhotoGrid.tsx that wraps VirtualizedMasonryGrid with photo rendering logic.

    Key implementation:
    - Accept photos: Photo[], onPhotoClick, onLike, selectMode, selectedIds, onToggleSelect props
    - Use VirtualizedMasonryGrid for layout
    - Render each photo with motion.div (reuse existing hover animations from PhotoGrid)
    - Photo cards: copy the existing styling from MasonryPhotoGrid in PhotoGrid.tsx (shadow, border-radius, hover effects)
    - PhotoLikeButton: reuse from PhotoGrid.tsx
    - SelectOverlay: reuse from PhotoGrid.tsx
    - onPhotoClick handler: calls onPhotoClick(photo, globalIndex) where globalIndex is calculated from row position + position within row

    Prefetching per D-04:
    - Maintain a prefetchMap: Map<string, string> for preloaded photo URLs
    - When a photo comes into view range, prefetch ±5 photos around current position (11 total)
    - Use link rel="prefetch" for prefetching (same pattern as galleryStore prefetchAdjacentImages)
    - Store preloaded URLs in the Map; lightbox can use cached instead of fetching

    The component must handle the index mapping correctly:
    - photos array is flat; need to calculate which row each photo belongs to
    - globalIndex = rowStartIndex + positionWithinRow
    - onPhotoClick receives the global index for lightbox state

    Export type:
    ```typescript
    export function VirtualizedPhotoGrid(props: PhotoGridProps)
    ```
  </action>
  <verify>
    <automated>grep -q "VirtualizedMasonryGrid" src/components/gallery/VirtualizedPhotoGrid.tsx && grep -q "prefetch" src/components/gallery/VirtualizedPhotoGrid.tsx && echo "VIRTUALIZED_PHOTO_GRID_OK"</automated>
  </verify>
  <done>VirtualizedPhotoGrid component created with prefetching (±5 photos, 11 total), PhotoLikeButton, SelectOverlay, and correct index mapping</done>
</task>

</tasks>

<verification>
- @tanstack/react-virtual is installed and importable
- VirtualizedMasonryGrid.tsx exists with useVirtualizer, MASONRY_COLUMNS breakpoints, ResizeObserver, hasFixedSize=false
- VirtualizedPhotoGrid.tsx exists with prefetching logic and index mapping
- No console.log or console.error in production code
- All existing PhotoGrid props are supported (onPhotoClick, onLike, selectMode, selectedIds, onToggleSelect)
</verification>

<success_criteria>
1. VirtualizedPhotoGrid renders with @tanstack/react-virtual using row-based masonry
2. Only visible rows are rendered in DOM (virtualization working)
3. Lightbox opens for any virtualized photo via onPhotoClick
4. Adjacent photos (±5, 11 total) are prefetched
5. Gallery scrolls smoothly with 200+ photos (test with large dataset)
</success_criteria>

<output>
After completion, create `.planning/phases/07-gallery-virtualization/07-01-SUMMARY.md`
</output>