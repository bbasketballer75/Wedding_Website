---
phase: 13
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/gallery/components/GalleryHeader.tsx
  - src/components/search/Search.tsx
  - src/components/admin/BatchList.tsx
  - src/pages/admin/AuditLogView.tsx
  - src/components/ui/UIComponents.test.tsx
autonomous: true
requirements:
  - UX-11

must_haves:
  truths:
    - "All focus rings use gold CSS variable consistently"
    - "No hardcoded focus:ring-gold-* classes remain"
  artifacts:
    - path: "src/components/gallery/components/GalleryHeader.tsx"
      provides: "Gold focus ring on interactive elements"
      contains: "focus:ring-(--color-gold)"
    - path: "src/components/search/Search.tsx"
      provides: "Gold focus ring on search input"
      contains: "focus:ring-(--color-gold)"
    - path: "src/components/admin/BatchList.tsx"
      provides: "Gold focus ring on select input"
      contains: "focus:ring-(--color-gold)"
    - path: "src/pages/admin/AuditLogView.tsx"
      provides: "Gold focus ring on filter inputs"
      contains: "focus:ring-(--color-gold)"
  key_links:
    - from: "GalleryHeader.tsx:43,53,68"
      to: "CSS var --color-gold"
      via: "focus:ring-(--color-gold)"
      pattern: "focus:ring-gold-500/50.*focus:ring-(--color-gold)"
---

<objective>
Standardize all focus ring colors to use the gold CSS variable `focus:ring-(--color-gold)` per D-02 and D-03. Replace inconsistent `focus:ring-gold-500/50`, `focus:ring-gold-400`, `focus:ring-gold-500/20` patterns.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/components/gallery/components/GalleryHeader.tsx
@src/components/search/Search.tsx
@src/components/admin/BatchList.tsx
@src/pages/admin/AuditLogView.tsx
@.planning/phases/13-accessibility-motion/13-CONTEXT.md
@.planning/phases/13-accessibility-motion/13-UI-SPEC.md
</context>

<interfaces>
From DarkModeToggle.tsx (established pattern per 13-UI-SPEC.md lines 80-82):
```typescript
className='... focus:ring-2 focus:ring-offset-2 focus:ring-(--color-gold)'
```

This is the canonical pattern to follow - `focus:ring-(--color-gold)` CSS variable reference.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 0: Add automated test for focus ring CSS variable usage</name>
  <files>src/components/ui/UIComponents.test.tsx</files>
  <read_first>src/components/ui/UIComponents.test.tsx</read_first>
  <action>
Add a new describe block for focus ring verification tests. Add this at the end of the file (after line 67):

```typescript
describe('Focus Ring CSS Variable Verification', () => {
  const targetFiles = [
    'src/components/gallery/components/GalleryHeader.tsx',
    'src/components/search/Search.tsx',
    'src/components/admin/BatchList.tsx',
    'src/pages/admin/AuditLogView.tsx',
  ]

  it('uses focus:ring-(--color-gold) instead of hardcoded gold classes', () => {
    targetFiles.forEach(file => {
      const content = fs.readFileSync(file, 'utf-8')
      // Should NOT contain hardcoded focus:ring-gold-* patterns
      expect(content).not.toMatch(/focus:ring-gold-\d+/)
      expect(content).not.toMatch(/focus:ring-gold-\d+\/\d+/)
      // SHOULD contain the CSS variable pattern
      expect(content).toMatch(/focus:ring-\(--color-gold\)/)
    })
  })
})
```

Note: This test reads source files directly to verify the CSS class patterns. The test ensures no hardcoded gold focus ring classes exist and the CSS variable is used consistently.
  </action>
  <verify>
npm run test:run -- --grep "Focus Ring" 2>&1 | head -30
  </verify>
  <done>Automated test added to verify focus:ring-(--color-gold) usage across target files</done>
</task>

<task type="auto">
  <name>Task 1: Fix focus rings in GalleryHeader.tsx</name>
  <files>src/components/gallery/components/GalleryHeader.tsx</files>
  <read_first>src/components/gallery/components/GalleryHeader.tsx</read_first>
  <action>
Replace three focus ring usages in GalleryHeader.tsx:

**Line 43** (search input):
- FROM: `focus:ring-2 focus:ring-gold-500/50 focus:border-gold-500/50`
- TO: `focus:ring-2 focus:ring-(--color-gold) focus:border-(--color-gold)`

**Line 53** (filter select):
- FROM: `focus:ring-2 focus:ring-gold-500/50`
- TO: `focus:ring-2 focus:ring-(--color-gold)`

**Line 68** (sort select):
- FROM: `focus:ring-2 focus:ring-gold-500/50`
- TO: `focus:ring-2 focus:ring-(--color-gold)`

**Note:** Per D-03, focus rings are applied per-component inline, not via global CSS. Each interactive element declares its own focus ring style using the CSS variable.
  </action>
  <verify>
grep -n "focus:ring-" src/components/gallery/components/GalleryHeader.tsx
  </verify>
  <done>GalleryHeader.tsx uses focus:ring-(--color-gold) consistently on all interactive elements</done>
</task>

<task type="auto">
  <name>Task 2: Fix focus rings in Search.tsx</name>
  <files>src/components/search/Search.tsx</files>
  <read_first>src/components/search/Search.tsx</read_first>
  <action>
Replace focus ring usage in Search.tsx line 322:

**Line 322** (search input):
- FROM: `focus:ring-2 focus:ring-gold-400/20`
- TO: `focus:ring-2 focus:ring-(--color-gold)`

The input also has `focus:border-gold-400` which should be changed to `focus:border-(--color-gold)` for consistency.
  </action>
  <verify>
grep -n "focus:ring-\|focus:border-" src/components/search/Search.tsx | head -10
  </verify>
  <done>Search.tsx uses focus:ring-(--color-gold) consistently</done>
</task>

<task type="auto">
  <name>Task 3: Fix focus rings in BatchList.tsx and AuditLogView.tsx</name>
  <files>
    - src/components/admin/BatchList.tsx
    - src/pages/admin/AuditLogView.tsx
  </files>
  <read_first>
    - src/components/admin/BatchList.tsx
    - src/pages/admin/AuditLogView.tsx
  </read_first>
  <action>
**BatchList.tsx line 95** (select input):
- FROM: `focus:ring-2 focus:ring-gold-500/20`
- TO: `focus:ring-2 focus:ring-(--color-gold)`

**AuditLogView.tsx lines 87, 97, 110** (filter inputs):
- FROM: `focus:ring-2 focus:ring-gold-500/20`
- TO: `focus:ring-2 focus:ring-(--color-gold)`

All three locations in AuditLogView.tsx need the same fix.
  </action>
  <verify>
grep -n "focus:ring-gold" src/components/admin/BatchList.tsx src/pages/admin/AuditLogView.tsx
  </verify>
  <done>BatchList.tsx and AuditLogView.tsx use focus:ring-(--color-gold) consistently</done>
</task>

</tasks>

<verification>
- `grep -rn "focus:ring-gold-500/50\|focus:ring-gold-400\|focus:ring-gold-500/20\|focus:ring-gold-500\b" src/components/` returns no results
- `grep -n "focus:ring-(--color-gold)" src/components/gallery/components/GalleryHeader.tsx src/components/search/Search.tsx src/components/admin/BatchList.tsx src/pages/admin/AuditLogView.tsx` finds all standardized focus rings
- `npm run test:run -- --grep "Focus Ring"` passes
- Build passes: `npm run build` (verify no Tailwind validation warnings)
</verification>

<success_criteria>
- All focus ring inconsistencies resolved: no more `focus:ring-gold-500/50`, `focus:ring-gold-400`, `focus:ring-gold-500/20`
- All focus rings now use `focus:ring-(--color-gold)` CSS variable
- Consistent with DarkModeToggle.tsx pattern (established canonical reference)
- `npm run lint` passes
- Automated test verifies CSS variable usage across target files
</success_criteria>

<output>
After completion, create `.planning/phases/13-accessibility-motion/13-02-SUMMARY.md`
</output>