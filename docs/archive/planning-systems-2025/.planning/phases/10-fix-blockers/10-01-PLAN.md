---
phase: "10"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/layout/BackgroundMusic.tsx
autonomous: true
requirements:
  - UX-01
must_haves:
  truths:
    - "BackgroundMusic.tsx no longer uses invalid z-100 class"
    - "Build produces no Tailwind class validation errors for BackgroundMusic.tsx"
  artifacts:
    - path: "src/components/layout/BackgroundMusic.tsx"
      provides: "BackgroundMusic component with valid z-index"
      contains: "z-50"
      not_contains: "z-100"
  key_links:
    - from: "src/components/layout/BackgroundMusic.tsx"
      to: "Tailwind z-index scale"
      via: "className replacement"
      pattern: "z-50 instead of z-100"
---

<objective>
Fix invalid Tailwind z-index class in BackgroundMusic.tsx.

Purpose: Eliminate build warnings by replacing invalid `z-100` with valid `z-50` (Tailwind's max z-index).
Output: BackgroundMusic.tsx with corrected z-index class.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/components/layout/BackgroundMusic.tsx
</context>

<interfaces>
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Replace invalid z-100 with z-50 in BackgroundMusic.tsx</name>
  <read_first>src/components/layout/BackgroundMusic.tsx</read_first>
  <action>
    In src/components/layout/BackgroundMusic.tsx line 182, replace the invalid Tailwind class:

    CHANGE:
    `className='fixed bottom-4 left-1/2 z-100 w-[calc(100vw-1.5rem)] ...`

    TO:
    `className='fixed bottom-4 left-1/2 z-50 w-[calc(100vw-1.5rem)] ...`

    The valid Tailwind z-index scale only goes up to z-50. Replace z-100 with z-50.
  </action>
  <verify>
    <automated>grep -n "z-100" src/components/layout/BackgroundMusic.tsx || echo "PASS: z-100 not found"</automated>
  </verify>
  <done>
    BackgroundMusic.tsx line 182 uses z-50 (not z-100), and grep confirms no z-100 remains.
  </done>
  <acceptance_criteria>
    - `grep -n "z-100" src/components/layout/BackgroundMusic.tsx` returns no matches
    - `grep -n "z-50" src/components/layout/BackgroundMusic.tsx` returns match on line 182
    - Build completes without Tailwind validation errors for this file
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | This is a simple CSS class fix with no security implications |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | N/A | BackgroundMusic.tsx | accept | No security impact - CSS class correction |

</threat_model>

<verification>
- `npm run build` produces no Tailwind class validation errors
- `grep -n "z-100" src/components/layout/BackgroundMusic.tsx` returns no matches
</verification>

<success_criteria>
1. BackgroundMusic.tsx no longer contains `z-100`
2. BackgroundMusic.tsx uses `z-50` on line 182
3. Build completes successfully without Tailwind z-index warnings
</success_criteria>

<output>
After completion, create `.planning/phases/10-fix-blockers/10-01-SUMMARY.md`
</output>