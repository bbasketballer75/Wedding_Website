---
phase: "10"
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/error/ErrorBoundary.tsx
autonomous: true
requirements:
  - UX-02
must_haves:
  truths:
    - "ErrorBoundary.tsx no longer uses invalid bg-(--color-gold) CSS var syntax"
    - "Build produces no Tailwind class validation errors for ErrorBoundary.tsx"
  artifacts:
    - path: "src/components/error/ErrorBoundary.tsx"
      provides: "ErrorBoundary component with valid gold background color"
      contains: "bg-gold-500"
      not_contains: "bg-(--color-gold)"
  key_links:
    - from: "src/components/error/ErrorBoundary.tsx"
      to: "src/tokens/designTokens.ts"
      via: "gold-500 color token"
      pattern: "bg-gold-500 from design token scale"
---

<objective>
Fix invalid CSS var syntax in ErrorBoundary.tsx.

Purpose: Replace invalid `bg-(--color-gold)` and `focus:ring-(--color-gold)` with valid Tailwind classes using the gold color token scale (gold-500 is the primary brand gold at #c9a05c).
Output: ErrorBoundary.tsx with corrected background and focus ring classes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/components/error/ErrorBoundary.tsx
@src/tokens/designTokens.ts
</context>

<interfaces>
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Replace invalid CSS var syntax with valid gold-500 token</name>
  <read_first>src/components/error/ErrorBoundary.tsx</read_first>
  <action>
    In src/components/error/ErrorBoundary.tsx, replace all instances of invalid CSS var syntax:

    CHANGE (line 26):
    `<div className='w-16 h-1 bg-(--color-gold) mx-auto mb-6'></div>`

    TO:
    `<div className='w-16 h-1 bg-gold-500 mx-auto mb-6'></div>`

    CHANGE (line 55):
    `className='px-6 py-3 bg-(--color-gold) text-white rounded-md ... focus:ring-(--color-gold) ...'`

    TO:
    `className='px-6 py-3 bg-gold-500 text-white rounded-md ... focus:ring-gold-500 ...'`

    The gold color scale in designTokens.ts defines gold-500 (#c9a05c) as the primary brand gold. Use `bg-gold-500` and `focus:ring-gold-500` instead of the invalid CSS var syntax.
  </action>
  <verify>
    <automated>grep -n "bg-(--color-gold)" src/components/error/ErrorBoundary.tsx || echo "PASS: bg-(--color-gold) not found"</automated>
  </verify>
  <done>
    ErrorBoundary.tsx uses bg-gold-500 (not bg-(--color-gold)) and focus:ring-gold-500 (not focus:ring-(--color-gold)), and grep confirms no invalid CSS var syntax remains.
  </done>
  <acceptance_criteria>
    - `grep -n "bg-(--color-gold)" src/components/error/ErrorBoundary.tsx` returns no matches
    - `grep -n "focus:ring-(--color-gold)" src/components/error/ErrorBoundary.tsx` returns no matches
    - `grep -n "bg-gold-500" src/components/error/ErrorBoundary.tsx` returns matches at lines 26 and 55
    - `grep -n "focus:ring-gold-500" src/components/error/ErrorBoundary.tsx` returns match at line 55
    - Build completes without Tailwind validation errors for this file
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | This is a simple CSS class fix with no security implications |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-02 | N/A | ErrorBoundary.tsx | accept | No security impact - CSS class correction |

</threat_model>

<verification>
- `npm run build` produces no Tailwind class validation errors
- `grep -n "bg-(--color-gold)" src/components/error/ErrorBoundary.tsx` returns no matches
- `grep -n "focus:ring-(--color-gold)" src/components/error/ErrorBoundary.tsx` returns no matches
</verification>

<success_criteria>
1. ErrorBoundary.tsx no longer contains `bg-(--color-gold)` or `focus:ring-(--color-gold)`
2. ErrorBoundary.tsx uses `bg-gold-500` at line 26 and line 55
3. Build completes successfully without Tailwind class validation warnings
</success_criteria>

<output>
After completion, create `.planning/phases/10-fix-blockers/10-02-SUMMARY.md`
</output>