---
phase: 14-accessibility-visual
plan: 03
type: execute
wave: 2
depends_on: ["14-01", "14-02"]
files_modified:
  - src/contexts/ToastContext.tsx
  - src/components/ui/VideoPlayer.tsx
  - src/components/gallery/MapView.tsx
  - src/components/sections/LoveTimeline.tsx
  - src/components/layout/LocationMap.tsx
autonomous: true
requirements:
  - UX-15

must_haves:
  truths:
    - "No hardcoded hex values in modified files"
    - "All colors use CSS variable references from designTokens"
    - "Conditional theme components (HalloweenEngagement.tsx) preserve intentional themes"
  artifacts:
    - path: "src/contexts/ToastContext.tsx"
      provides: "Toast notifications using CSS variables"
    - path: "src/components/ui/VideoPlayer.tsx"
      provides: "Video player using CSS variables for cinematic theme"
    - path: "src/components/gallery/MapView.tsx"
      provides: "Map markers using gold CSS variable"
    - path: "src/components/sections/LoveTimeline.tsx"
      provides: "Timeline using CSS variables for rose/candle colors"
    - path: "src/components/layout/LocationMap.tsx"
      provides: "Location map using gold CSS variables"
  key_links:
    - from: "designTokens.ts"
      to: "ToastContext.tsx, VideoPlayer.tsx, etc."
      via: "--color-gold-500, --color-cream-*, --color-mocha-*"
    - from: "index.css"
      to: "all component files"
      via: "@theme CSS variables"
---

<objective>
Comprehensive color token audit and fix per D-05 (comprehensive sweep scope) and D-06 (includes hardcoded hex, incorrect CSS var refs, and unused tokens).

**D-05:** Find and fix every color usage that doesn't use design tokens correctly.
**D-06:** Audit includes hardcoded hex values, incorrect CSS variable references, and places where tokens exist but aren't being used.

*Preserve intentional conditional theme colors (e.g., HalloweenEngagement.tsx uses spooky theme on purpose).*
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/tokens/designTokens.ts
@src/index.css

From RESEARCH.md (color token audit findings):
| File | Hex Values Found | Should Use |
|------|------------------|------------|
| ToastContext.tsx | `#050508`, `#fff7eb` | `--color-charcoal-900`, `--color-candle-100` |
| VideoPlayer.tsx | `#130e0b`, `#dbb880` | `--color-mocha-900`, `--color-gold-400` |
| MapView.tsx | `#d2b178` | `--color-gold-400` |
| LoveTimeline.tsx | `#DC2626`, `#7F1D1D`, `#991B1B`, `#fff7eb`, `#f9a8d4` | `--color-rose-*`, `--color-candle-*` |
| LocationMap.tsx | `#B08D46`, `#D4C4A8` | `--color-gold-600`, `--color-cream-200` |

CSS variable reference patterns (from index.css @theme):
- `bg-[var(--color-gold-500)]` — explicit CSS var in Tailwind
- `bg-gold-500` — Tailwind shorthand (works for standard cases)
- `text-charcoal-700` — Tailwind shorthand
- `bg-cream-50` — Tailwind shorthand

Gold brand mapping: `#d4af37` → `--color-gold-500`

**IMPORTANT - Conditional themes:** Files like `HalloweenEngagement.tsx` use intentional hex values for spooky theme. Do NOT replace these — they are conditional themes, not inconsistencies.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix ToastContext.tsx hardcoded hex values</name>
  <files>src/contexts/ToastContext.tsx</files>
  <action>
**Read first:** `src/contexts/ToastContext.tsx`

Find hardcoded hex values:
- `#050508` → replace with `--color-charcoal-900` or `bg-charcoal-900`
- `#fff7eb` → replace with `--color-candle-100` or `bg-candle-100`

Search pattern: `#[0-9a-fA-F]{3,6}`

Apply CSS var references using Tailwind shorthand (preferred) or explicit `var()` syntax:
- For backgrounds: `bg-charcoal-900`, `bg-candle-100`
- For text: `text-charcoal-900`, `text-candle-100`
- For borders: `border-charcoal-900`, `border-candle-100`

Do NOT change: any hex that is part of a gradient or complex effect that uses CSS properly.
  </action>
  <verify>
    <automated>grep -E "#[0-9a-fA-F]{3,6}" src/contexts/ToastContext.tsx | grep -v "var(" | wc -l</automated>
  </verify>
  <done>ToastContext.tsx has no hardcoded hex values — all colors use CSS variable references.</done>
</task>

<task type="auto">
  <name>Task 2: Fix VideoPlayer.tsx hardcoded hex values</name>
  <files>src/components/ui/VideoPlayer.tsx</files>
  <action>
**Read first:** `src/components/ui/VideoPlayer.tsx`

Find hardcoded hex values:
- `#130e0b` → replace with `--color-mocha-900` or `bg-mocha-900`
- `#dbb880` → replace with `--color-gold-400` or `bg-gold-400`

Check for any other hex values in the file.

Use Tailwind shorthand: `bg-mocha-900`, `bg-gold-400`, `text-mocha-900`, `text-gold-400`.
  </action>
  <verify>
    <automated>grep -E "#[0-9a-fA-F]{3,6}" src/components/ui/VideoPlayer.tsx | grep -v "var(" | wc -l</automated>
  </verify>
  <done>VideoPlayer.tsx has no hardcoded hex values — all colors use CSS variable references.</done>
</task>

<task type="auto">
  <name>Task 3: Fix MapView.tsx hardcoded hex values</name>
  <files>src/components/gallery/MapView.tsx</files>
  <action>
**Read first:** `src/components/gallery/MapView.tsx`

Find hardcoded hex values:
- `#d2b178` → replace with `--color-gold-400` or `bg-gold-400`

This is used for map marker styling.

Use Tailwind shorthand: `bg-gold-400` for the marker color.
  </action>
  <verify>
    <automated>grep -E "#[0-9a-fA-F]{3,6}" src/components/gallery/MapView.tsx | grep -v "var(" | wc -l</automated>
  </verify>
  <done>MapView.tsx has no hardcoded hex values — all colors use CSS variable references.</done>
</task>

<task type="auto">
  <name>Task 4: Fix LoveTimeline.tsx hardcoded hex values</name>
  <files>src/components/sections/LoveTimeline.tsx</files>
  <action>
**Read first:** `src/components/sections/LoveTimeline.tsx`

Find hardcoded hex values:
- `#DC2626`, `#7F1D1D`, `#991B1B` (reds) → use `--color-rose-500`, `--color-rose-700`, `--color-rose-800`
- `#fff7eb` → use `--color-candle-100` or `bg-candle-100`
- `#f9a8d4` (pink) → use `--color-rose-200` or `bg-rose-200`

This is a timeline component with candle/romantic theme — apply appropriate rose/candle colors.
  </action>
  <verify>
    <automated>grep -E "#[0-9a-fA-F]{3,6}" src/components/sections/LoveTimeline.tsx | grep -v "var(" | wc -l</automated>
  </verify>
  <done>LoveTimeline.tsx has no hardcoded hex values — all colors use CSS variable references.</done>
</task>

<task type="auto">
  <name>Task 5: Fix LocationMap.tsx hardcoded hex values</name>
  <files>src/components/layout/LocationMap.tsx</files>
  <action>
**Read first:** `src/components/layout/LocationMap.tsx`

Find hardcoded hex values:
- `#B08D46` → use `--color-gold-600` or `bg-gold-600`
- `#D4C4A8` → use `--color-cream-300` or `bg-cream-300`

Use Tailwind shorthand for gold and cream tones.
  </action>
  <verify>
    <automated>grep -E "#[0-9a-fA-F]{3,6}" src/components/layout/LocationMap.tsx | grep -v "var(" | wc -l</automated>
  </verify>
  <done>LocationMap.tsx has no hardcoded hex values — all colors use CSS variable references.</done>
</task>

</tasks>

<verification>
Run comprehensive audit across all component files:
```bash
grep -rE "#[0-9a-fA-F]{3,6}" src/ --include="*.tsx" | grep -v "HalloweenEngagement\|conditional\|theme" | grep -v "var(" | head -20
```
Any matches outside conditional theme files need investigation.
</verification>

<success_criteria>
1. ToastContext.tsx: `#050508` → charcoal-900, `#fff7eb` → candle-100
2. VideoPlayer.tsx: `#130e0b` → mocha-900, `#dbb880` → gold-400
3. MapView.tsx: `#d2b178` → gold-400
4. LoveTimeline.tsx: reds → rose-*, `#fff7eb` → candle-100, pink → rose-200
5. LocationMap.tsx: `#B08D46` → gold-600, `#D4C4A8` → cream-300
6. No hardcoded hex values remaining in modified files
7. Conditional theme files (HalloweenEngagement.tsx) preserved with intentional hex
</success_criteria>

<output>
After completion, create `.planning/phases/14-accessibility-visual/14-03-SUMMARY.md`
</output>