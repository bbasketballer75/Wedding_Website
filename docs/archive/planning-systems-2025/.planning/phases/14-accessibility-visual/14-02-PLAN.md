---
phase: 14-accessibility-visual
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/CustomCursor.tsx
  - src/components/layout/BackgroundMusic.tsx
  - src/components/gallery/Lightbox.tsx
autonomous: true
requirements:
  - UX-14

must_haves:
  truths:
    - "Framer Motion durations conform to three-tier system"
    - "Tailwind transition durations use consistent values"
    - "Micro: 150ms, Transitions: 300ms, Animations: 500ms+"
  artifacts:
    - path: "src/components/ui/CustomCursor.tsx"
      provides: "Cursor animation with standardized durations"
    - path: "src/components/layout/BackgroundMusic.tsx"
      provides: "Background music toggle with standardized transitions"
    - path: "src/components/gallery/Lightbox.tsx"
      provides: "Lightbox with standardized animation durations"
  key_links:
    - from: "Phase 13 UI-SPEC.md"
      to: "CustomCursor.tsx, BackgroundMusic.tsx"
      via: "duration: 0.15, duration: 0.3, duration: 0.5"
---

<objective>
Standardize Framer Motion animation durations and Tailwind transition durations per D-03 (three-tier system) and D-04 (DarkModeToggle verified at 300ms).

**D-03:** Three-tier duration system:
- Micro: 150ms (`duration: 0.15`, `duration-150`) — hover states, button presses
- Transitions: 300ms (`duration: 0.3`, `duration-300`) — theme toggle, modal open, page transitions
- Animations: 500ms+ (`duration: 0.5`, `duration-500`) — complex entrance/exit animations

**D-04:** DarkModeToggle stays at `duration: 0.3` — already correct, no action needed.

*Verify existing durations across components, update inconsistent values to match tiers.*
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/components/ui/DarkModeToggle.tsx
@src/components/ui/CustomCursor.tsx

From Phase 13 UI-SPEC.md (animation duration baseline):
| Type | Duration | Framer Motion | Tailwind |
|------|----------|---------------|----------|
| Micro | 150ms | `duration: 0.15` | `duration-150` |
| Transitions | 300ms | `duration: 0.3` | `duration-300` |
| Animations | 500ms+ | `duration: 0.5` | `duration-500` |

D-04 verified: DarkModeToggle.tsx already uses `duration: 0.3` — no changes needed.

Duration tier mapping:
| Current value | Target tier |
|--------------|-------------|
| 0.1, 0.15 | Micro (150ms) |
| 0.2, 0.22, 0.25 | Micro or Transitions depending on use |
| 0.28, 0.3, 0.35, 0.4 | Transitions (300ms) |
| 0.5, 0.6 | Animations (500ms+) |
| 0.8, 1.0, 1.5+ | Animations (500ms+) — keep as-is for complex |

Use case classification:
- Hover/focus states → Micro (150ms)
- Theme toggles, modal open/close → Transitions (300ms)
- Page transitions, entrance animations → Animations (500ms+)
- Complex orchestration (staggered children) → Animations (500ms+)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit and fix CustomCursor durations</name>
  <files>src/components/ui/CustomCursor.tsx</files>
  <action>
**Read first:** `src/components/ui/CustomCursor.tsx`

Find all `transition={{ duration: X }}` in Framer Motion components.

Apply tier mapping:
- Hover states, cursor size changes → `duration: 0.15` (Micro)
- Visibility fade → `duration: 0.3` (Transitions)

Check specifically for:
- `duration: 0.2` → change to `duration: 0.15` (Micro for hover)
- `duration: 0.5` on visibility → change to `duration: 0.3` (Transitions for fade)
- Any `duration: 0.1` → keep as Micro

Only change durations, do not change `ease` values.
  </action>
  <verify>
    <automated>grep "duration:" src/components/ui/CustomCursor.tsx | grep -v "0.15\|0.3"</automated>
  </verify>
  <done>CustomCursor.tsx all durations conform to tier system (150ms micro or 300ms transitions).</done>
</task>

<task type="auto">
  <name>Task 2: Audit and fix BackgroundMusic durations</name>
  <files>src/components/layout/BackgroundMusic.tsx</files>
  <action>
**Read first:** `src/components/layout/BackgroundMusic.tsx`

Find all `transition={{ duration: X }}` and `transitionDuration` patterns.

Apply tier mapping:
- Button hover states → `duration: 0.15` (Micro)
- Panel/modal open transitions → `duration: 0.3` (Transitions)
- Icon rotations, decorative → `duration: 0.5` (Animations)

Check for non-standard values like:
- `duration: 0.2` on hover → `duration: 0.15`
- `duration: 0.4` on transitions → `duration: 0.3`
- `duration: 0.25` → evaluate based on use case
  </action>
  <verify>
    <automated>grep -E "duration:[0-9]" src/components/layout/BackgroundMusic.tsx | grep -v "0.15\|0.3\|0.5"</automated>
  </verify>
  <done>BackgroundMusic.tsx all durations conform to tier system.</done>
</task>

<task type="auto">
  <name>Task 3: Audit and fix Lightbox durations</name>
  <files>src/components/gallery/Lightbox.tsx</files>
  <action>
**Read first:** `src/components/gallery/Lightbox.tsx`

Find all `transition={{ duration: X }}` in Framer Motion usages.

Apply tier mapping:
- Image scale on hover → `duration: 0.15` (Micro)
- Lightbox open/close → `duration: 0.3` (Transitions)
- Navigation transitions → `duration: 0.3` (Transitions)
- Complex image gallery animations → `duration: 0.5` (Animations)

Lightbox specific:
- `duration: 0.2` on image hover → `duration: 0.15`
- `duration: 0.35` on modal open → `duration: 0.3`
- `duration: 0.4` → evaluate, likely `duration: 0.3` or `duration: 0.5`
  </action>
  <verify>
    <automated>grep -E "duration:[0-9]" src/components/gallery/Lightbox.tsx | grep -v "0.15\|0.3\|0.5"</automated>
  </verify>
  <done>Lightbox.tsx all durations conform to tier system.</done>
</task>

</tasks>

<verification>
Run across all modified files to confirm tier compliance:
```bash
grep -r "duration: 0\.[0-9]" src/components/ --include="*.tsx" -h | sort | uniq -c | sort -rn
```
Check output: should only show 0.15, 0.3, 0.5 (and potentially 0.6+ for complex animations which are acceptable as Animations tier).
</verification>

<success_criteria>
1. CustomCursor.tsx: hover states use 150ms, visibility uses 300ms
2. BackgroundMusic.tsx: hover uses 150ms, transitions use 300ms
3. Lightbox.tsx: hover uses 150ms, open/close uses 300ms
4. No durations like 0.2, 0.22, 0.28, 0.35, 0.4 remaining in modified files
5. DarkModeToggle.tsx: unchanged (already correct per D-04)
6. Complex animations (500ms+) kept as-is for staggered/orchestrated effects
</success_criteria>

<output>
After completion, create `.planning/phases/14-accessibility-visual/14-02-SUMMARY.md`
</output>