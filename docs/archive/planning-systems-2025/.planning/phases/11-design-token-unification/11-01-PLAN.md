---
phase: "11"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/tokens/designTokens.ts
  - src/index.css
autonomous: true
requirements:
  - UX-05 (part 1: shadow-gold alignment)
  - D-01 (gold-500 alignment)
must_haves:
  truths:
    - "designTokens.ts gold-500 is aligned to brand gold #d4af37"
    - "index.css gold-500 is aligned to brand gold #d4af37"
    - "index.css shadow-gold uses brand gold #d4af37"
  artifacts:
    - path: "src/tokens/designTokens.ts"
      provides: "gold-500 aligned to brand gold"
      contains: "500: '#d4af37'"
      not_contains: "500: '#c9a05c'"
    - path: "src/index.css"
      provides: "gold-500 and shadow-gold aligned"
      contains: "--color-gold-500: #d4af37"
      not_contains: "--color-gold-500: #c9a05c"
---

<objective>
Align design token gold values with the brand gold (#d4af37) used throughout the site.

Purpose: Resolve the conflict where designTokens.ts and index.css use #c9a05c but themes/index.ts uses #d4af37 as the canonical brand gold. This ensures all gold tokens reference the correct brand color.
Output: designTokens.ts and index.css with gold-500 = #d4af37.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/tokens/designTokens.ts
@src/index.css
@src/themes/index.ts
</context>

<interfaces>
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Align designTokens.ts gold-500 to #d4af37</name>
  <read_first>src/tokens/designTokens.ts</read_first>
  <action>
    In src/tokens/designTokens.ts line 11, change:
    `500: '#c9a05c', // Primary Brand Gold`
    TO:
    `500: '#d4af37', // Primary Brand Gold`

    This aligns the TypeScript design token with the brand gold used in themes/index.ts and throughout the site.
  </action>
  <verify>
    <automated>grep -n "#c9a05c" src/tokens/designTokens.ts || echo "PASS: old gold not found"</automated>
  </verify>
  <done>
    designTokens.ts gold-500 is now #d4af37 (matching brand gold)
  </done>
  <acceptance_criteria>
    - `grep -n "#c9a05c" src/tokens/designTokens.ts` returns no matches
    - `grep -n "#d4af37" src/tokens/designTokens.ts` returns match on line 11
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Align index.css gold-500 to #d4af37</name>
  <read_first>src/index.css</read_first>
  <action>
    In src/index.css line 29, change:
    `--color-gold-500: #c9a05c;`
    TO:
    `--color-gold-500: #d4af37;`

    This aligns the CSS variable with the brand gold.
  </action>
  <verify>
    <automated>grep -n "#c9a05c" src/index.css || echo "PASS: old gold not found"</automated>
  </verify>
  <done>
    index.css gold-500 is now #d4af37 (matching brand gold)
  </done>
  <acceptance_criteria>
    - `grep -n "#c9a05c" src/index.css` returns no matches
    - `grep -n "#d4af37" src/index.css` returns match on line 29
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Update shadow-gold in index.css to use aligned brand gold</name>
  <read_first>src/index.css</read_first>
  <action>
    In src/index.css line 93, the shadow-gold is defined as:
    `--shadow-gold: 0 4px 20px rgba(201, 160, 92, 0.25);`

    The rgba value rgba(201, 160, 92) corresponds to the OLD gold #c9a05c. Update it to use the brand gold #d4af37 converted to rgb:
    rgba(212, 175, 55) = rgb(212, 175, 55) from #d4af37

    CHANGE:
    `--shadow-gold: 0 4px 20px rgba(201, 160, 92, 0.25);`
    TO:
    `--shadow-gold: 0 4px 20px rgba(212, 175, 55, 0.25);`
  </action>
  <verify>
    <automated>grep -n "shadow-gold" src/index.css | grep "rgba(212, 175, 55" || echo "PASS: shadow-gold uses new gold"</automated>
  </verify>
  <done>
    index.css shadow-gold now uses brand gold rgba(212, 175, 55, 0.25)
  </done>
  <acceptance_criteria>
    - `grep -n "shadow-gold" src/index.css` shows rgba(212, 175, 55, 0.25)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | This is a color value alignment task with no security implications |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | N/A | designTokens.ts/index.css | accept | Color value correction only |

</threat_model>

<verification>
- `grep -rn "#c9a05c" src/tokens/designTokens.ts src/index.css` returns no matches
- `grep -n "#d4af37" src/tokens/designTokens.ts` returns match on line 11
- `grep -n "#d4af37" src/index.css` returns match on line 29
- `npm run build` produces no errors
</verification>

<success_criteria>
1. designTokens.ts gold-500 = #d4af37
2. index.css --color-gold-500 = #d4af37
3. index.css --shadow-gold uses rgba(212, 175, 55, 0.25)
4. Build completes without errors
</success_criteria>

<output>
After completion, update ROADMAP.md phase 11 progress.
</output>
