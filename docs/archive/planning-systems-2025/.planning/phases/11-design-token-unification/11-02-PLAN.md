---
phase: "11"
plan: "02"
type: execute
wave: 1
depends_on:
  - "11-01"
files_modified:
  - src/components/layout/Footer.tsx
autonomous: true
requirements:
  - UX-03
must_haves:
  truths:
    - "Footer.tsx uses no hardcoded hex colors"
    - "Footer.tsx uses only design token references (text-gold-*, bg-gold-*, etc.)"
  artifacts:
    - path: "src/components/layout/Footer.tsx"
      provides: "Footer component with design token colors only"
      not_contains: "text-[#|bg-[#|#[0-9a-fA-F]"
---

<objective>
Replace all hardcoded hex values in Footer.tsx with design token references.

Purpose: Per UX-03 requirement, eliminate hardcoded hex values in Footer.tsx that cause color inconsistency when design tokens are updated. The Footer already uses some gold tokens (gold-300, gold-400) but has inline hex for text colors.
Output: Footer.tsx with all colors using design token references.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/components/layout/Footer.tsx
@src/tokens/designTokens.ts
</context>

<interfaces>
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Replace text-[#f7e6c6] with text-gold-200</name>
  <read_first>src/components/layout/Footer.tsx</read_first>
  <action>
    In Footer.tsx line 42, replace the hardcoded hex text color:
    `className="text-[10px] uppercase tracking-[0.35em] text-[#f7e6c6]"`
    TO:
    `className="text-[10px] uppercase tracking-[0.35em] text-gold-200"`

    The hex #f7e6c6 is a light gold that matches gold-200 range. The standard gold-200 token is #f2e8d5, but looking at the usage context (eyebrow text), gold-200 is the closest standard token approximation.
  </action>
  <verify>
    <automated>grep -n "#f7e6c6" src/components/layout/Footer.tsx || echo "PASS: hardcoded hex not found"</automated>
  </verify>
  <done>
    Line 42 uses text-gold-200 instead of hardcoded hex
  </done>
  <acceptance_criteria>
    - `grep -n "#f7e6c6" src/components/layout/Footer.tsx` returns no matches
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Replace text-[#f2dfba] with text-gold-200/80</name>
  <read_first>src/components/layout/Footer.tsx</read_first>
  <action>
    In Footer.tsx line 54, replace:
    `className="mt-8 flex flex-wrap items-center gap-3 text-[11px] uppercase tracking-[0.28em] text-[#f2dfba]"`
    TO:
    `className="mt-8 flex flex-wrap items-center gap-3 text-[11px] uppercase tracking-[0.28em] text-gold-200/80"`

    The hex #f2dfba is a light gold similar to gold-200 but slightly warmer. Using gold-200 with opacity modifier provides a close visual match.
  </action>
  <verify>
    <automated>grep -n "#f2dfba" src/components/layout/Footer.tsx || echo "PASS: hardcoded hex not found"</automated>
  </verify>
  <done>
    Line 54 uses text-gold-200/80 instead of hardcoded hex
  </done>
  <acceptance_criteria>
    - `grep -n "#f2dfba" src/components/layout/Footer.tsx` returns no matches
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Replace text-[#fff3de] with text-gold-100</name>
  <read_first>src/components/layout/Footer.tsx</read_first>
  <action>
    In Footer.tsx line 87, replace:
    `className="flex flex-wrap gap-2 text-[11px] uppercase tracking-[0.22em] text-[#fff3de]"`
    TO:
    `className="flex flex-wrap gap-2 text-[11px] uppercase tracking-[0.22em] text-gold-100"`

    The hex #fff3de is a very light cream/gold - closest to gold-100 which is #f9f4eb. These are nearly identical warm cream tones.
  </action>
  <verify>
    <automated>grep -n "#fff3de" src/components/layout/Footer.tsx || echo "PASS: hardcoded hex not found"</automated>
  </verify>
  <done>
    Line 87 uses text-gold-100 instead of hardcoded hex
  </done>
  <acceptance_criteria>
    - `grep -n "#fff3de" src/components/layout/Footer.tsx` returns no matches
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Replace text-[#fff4e4] with text-gold-100/90</name>
  <read_first>src/components/layout/Footer.tsx</read_first>
  <action>
    In Footer.tsx lines 98 and 104, replace:
    `text-[#fff4e4]`
    TO:
    `text-gold-100/90`

    The hex #fff4e4 is a very light warm cream - closest to gold-100. Using gold-100 with opacity modifier.
  </action>
  <verify>
    <automated>grep -n "#fff4e4" src/components/layout/Footer.tsx || echo "PASS: hardcoded hex not found"</automated>
  </verify>
  <done>
    Lines 98 and 104 use text-gold-100/90 instead of hardcoded hex
  </done>
  <acceptance_criteria>
    - `grep -n "#fff4e4" src/components/layout/Footer.tsx` returns no matches
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | CSS class replacement with no security implications |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-02 | N/A | Footer.tsx | accept | Color class replacement only |

</threat_model>

<verification>
- `grep -rn "text-\[#\|bg-\[#\|#[0-9a-fA-F]" src/components/layout/Footer.tsx` returns no matches
- `npm run build` produces no Tailwind validation errors for Footer.tsx
</verification>

<success_criteria>
1. Footer.tsx contains no hardcoded hex values
2. All text colors use gold-* token scale
3. Build completes without errors
</success_criteria>

<output>
After completion, update ROADMAP.md phase 11 progress.
</output>
