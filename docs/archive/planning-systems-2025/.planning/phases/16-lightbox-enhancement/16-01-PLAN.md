---
phase: "16"
plan: "01"
type: "execute"
wave: 1
depends_on: []
files_modified:
  - "src/components/photo-viewer/PhotoLightbox.tsx"
autonomous: true
requirements:
  - "LB-01"
  - "LB-02"
  - "LB-03"
  - "LB-04"
must_haves:
  truths:
    - "User can pinch to zoom on mobile with range 1x to 3x"
    - "User can double-tap to toggle between 1x and 2x zoom"
    - "User can swipe left/right to navigate photos when zoomed at 1x"
    - "User can pan the image when zoomed > 1x (swipe does NOT navigate)"
    - "User can see date taken and camera info in the lightbox info panel"
    - "User can download the current photo from the lightbox toolbar"
  artifacts:
    - path: "src/components/photo-viewer/PhotoLightbox.tsx"
      provides: "Lightbox with pinch-to-zoom, swipe refinement, EXIF display, download button"
      exports:
        - "PhotoLightbox"
        - "zoom state"
        - "onDownload prop"
  key_links:
    - from: "useTouchGestures onPinch callback"
      to: "PhotoLightbox setZoom state"
      via: "scale calculation and Math.min/Math.max clamping"
    - from: "zoom state"
      to: "swipe behavior in onDragEnd"
      via: "zoom > 1 ? pan : navigate"
    - from: "currentPhoto.url"
      to: "Download button"
      via: "onDownload prop call with currentPhoto.id"
---

<objective>
Enhance the PhotoLightbox component with pinch-to-zoom (1x-3x), double-tap toggle (1x/2x), zoom-aware swipe navigation, EXIF metadata display, and download button. This delivers all 4 lightbox requirements (LB-01, LB-02, LB-03, LB-04) in a single plan.
</objective>

<context>
@src/components/photo-viewer/PhotoLightbox.tsx
@src/hooks/useTouchGestures.ts
@src/tokens/designTokens.ts
@.planning/REQUIREMENTS.md §LB
@.planning/phases/16-lightbox-enhancement/16-CONTEXT.md

## Key interfaces from PhotoLightbox.tsx

```typescript
interface PhotoLightboxProps {
  photos: Photo[]
  onLike?: (photoId: string) => void
  onShare?: (photoId: string) => void
  onDownload?: (photoId: string) => void  // Already exists - use for LB-04
  isDownloading?: boolean
  highlightedFaceName?: string | null
}

// Existing zoom state (line 58):
const [zoom, setZoom] = useState(1)

// Existing toolbar area (line 191-216):
<div className="flex items-center gap-2">
  {/* Zoom in/out buttons already exist - line 192-207 */}
</div>

// Existing info panel with showInfo toggle (line 391-516):
// Details tab shows caption, time/date, faces
// This is where EXIF data will display (LB-03)

// Existing onDragEnd (line 249-252):
onDragEnd={(_, info) => {
  if (info.offset.x < -50 && info.velocity.x < -80) handleNext()
  else if (info.offset.x > 50 && info.velocity.x > 80) handlePrevious()
}}
```

## Key interfaces from useTouchGestures.ts

```typescript
interface TouchGesturesOptions {
  onSwipeLeft?: (e: TouchEvent) => void
  onSwipeRight?: (e: TouchEvent) => void
  onPinch?: (scale: number, e: TouchEvent) => void  // Already defined - wire to setZoom
  onLongPress?: (e: TouchEvent) => void
  threshold?: number
  longPressDelay?: number
}

// Pinch scale is a multiplier (e.g., 1.5 = 150% zoom)
```

## Decisions from 16-CONTEXT.md

- D-01: Pinch-to-zoom with scale range 1x to 3x
- D-02: Double-tap toggles between 1x and 2x zoom (not 3x)
- D-03: Zoom uses Framer Motion for smooth animation (300ms ease-out)
- D-04: Zoom state stored in PhotoLightbox local state
- D-05: Swipe threshold: ~50px offset + velocity check
- D-06: When zoomed > 1x: swipe pans the image, does NOT navigate
- D-07: When zoomed = 1x: swipe navigates to next/previous photo
- D-08: Info panel slides in from right side (existing pattern)
- D-09: Shows: date taken, camera model, lens info if available
- D-10: Graceful fallback: if EXIF missing, show "No metadata available"
- D-11: Download button in lightbox toolbar (top right area)
- D-12: Downloads current photo at high quality via existing onDownload prop

## Animation Standards (Phase 13/14)

- D-13: Transitions: 300ms ease-out
- D-14: Micro-interactions: 150ms

## Design Tokens (Phase 11)

- D-15: Gold accent: #d4af37
- D-16: Cream backgrounds: cream-50, cream-100
- D-17: Border radius: rounded-xl for cards, rounded-lg for buttons
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire pinch-to-zoom and double-tap toggle to PhotoLightbox</name>
  <files>src/components/photo-viewer/PhotoLightbox.tsx</files>
  <read_first>
    - src/components/photo-viewer/PhotoLightbox.tsx (lines 1-100, focusing on state declarations and imports)
    - src/hooks/useTouchGestures.ts (lines 1-100, focusing on onPinch callback interface)
  </read_first>
  <action>
    1. Import useRef from 'react' (line 1 already has it)
    2. Add doubleTapTimer ref after the other refs (after line 102):
       const doubleTapTimer = useRef<{ x: number, y: number, time: number } | null>(null)
    3. Wire useTouchGestures to the image container div (find className="flex flex-1 items-center..." around line 243):
       - Add lightboxContentRef = useRef&lt;HTMLDivElement&gt;(null) before lightboxRef
       - Add useTouchGestures(lightboxContentRef, { onPinch: (scale, e) => { ... } })
    4. In the useEffect that initializes focus trapping (line 104), add touch gestures initialization:
       - After focus trap setup, call useTouchGestures(lightboxContentRef, { onPinch: (scale) => { setZoom(z => Math.min(Math.max(z * scale, 1), 3)) } })
    5. Add double-tap handler to the motion.img onClick (line 267):
       - On img click: detect double-tap (two clicks within 300ms, within 30px spatial distance)
       - Double-tap toggles: zoom === 1 ? setZoom(2) : setZoom(1)
    6. Ensure motion.div wrapping the image (line 254-308) uses drag="x" only when zoom === 1, otherwise drag disabled

    Key implementation details:
    - Pinch formula: setZoom(z => Math.min(Math.max(z * scale, 1), 3))
    - Double-tap: check if two taps within 300ms and within 30px
    - When zoom > 1, the existing drag="x" on the motion.div should be disabled via drag={zoom === 1 ? "x" : false}
    - The motion.div drag currently has dragElastic={0.12} which allows free panning when zoomed

    VERIFY: Search for "useTouchGestures" to confirm hook is wired. Search for "doubleTapTimer" to confirm double-tap logic. Search for "zoom === 1" or "zoom > 1" to confirm zoom-aware drag.
  </action>
  <verify>
    <automated>grep -n "useTouchGestures\|onPinch\|doubleTapTimer\|zoom === 1\|zoom > 1" src/components/photo-viewer/PhotoLightbox.tsx | head -20</automated>
  </verify>
  <acceptance_criteria>
    - [ ] useTouchGestures is imported and called with onPinch callback
    - [ ] onPinch updates zoom state: Math.min(Math.max(z * scale, 1), 3)
    - [ ] Double-tap handler exists and toggles between zoom 1 and 2
    - [ ] drag="x" is conditional on zoom === 1 (pan only when zoomed)
  </acceptance_criteria>
  <done>
    Pinch-to-zoom (1x-3x) and double-tap toggle (1x/2x) are wired to PhotoLightbox zoom state
  </done>
</task>

<task type="auto">
  <name>Task 2: Refine swipe navigation to be zoom-aware</name>
  <files>src/components/photo-viewer/PhotoLightbox.tsx</files>
  <read_first>
    - src/components/photo-viewer/PhotoLightbox.tsx (lines 243-310, the image container and onDragEnd)
  </read_first>
  <action>
    1. Modify the onDragEnd handler (currently lines 249-252):
       from:
       onDragEnd={(_, info) => {
         if (info.offset.x < -50 && info.velocity.x < -80) handleNext()
         else if (info.offset.x > 50 && info.velocity.x > 80) handlePrevious()
       }}
       to:
       onDragEnd={(_, info) => {
         // When zoomed > 1x, do NOT navigate - allow panning via dragElastic
         if (zoom > 1) return
         if (info.offset.x < -50 && info.velocity.x < -80) handleNext()
         else if (info.offset.x > 50 && info.velocity.x > 80) handlePrevious()
       }}

    2. Ensure the motion.div has drag={zoom === 1 ? "x" : false} and retains dragElastic={0.12} for panning when zoomed

    3. Update the swipe arrow buttons (handlePrevious/handleNext) to also check if zoomed > 1 and return early

    4. When navigating to next/previous photo, reset zoom to 1 (already done in handlePrevious/handleNext at lines 89 and 97)

    VERIFY: grep -n "zoom > 1" src/components/photo-viewer/PhotoLightbox.tsx should show the zoom-aware check
  </action>
  <verify>
    <automated>grep -n "zoom > 1\|handleNext\|handlePrevious" src/components/photo-viewer/PhotoLightbox.tsx | head -20</automated>
  </verify>
  <acceptance_criteria>
    - [ ] onDragEnd has explicit "if (zoom > 1) return" check
    - [ ] drag prop on motion.div is conditional: drag={zoom === 1 ? "x" : false}
    - [ ] Arrow button navigation also checks zoom level before navigating
  </acceptance_criteria>
  <done>
    Swipe navigation is zoom-aware: at zoom 1, swipe navigates; at zoom > 1, swipe pans the image
  </done>
</task>

<task type="auto">
  <name>Task 3: Add EXIF metadata display to lightbox info panel</name>
  <files>src/components/photo-viewer/PhotoLightbox.tsx</files>
  <read_first>
    - src/components/photo-viewer/PhotoLightbox.tsx (lines 420-475, the info tab content)
    - src/hooks/useExif.ts (if exists - check for existing EXIF parsing patterns)
  </read_first>
  <action>
    1. Check if exifr is already imported/used in the codebase. If not, add import at top:
       import exifr from 'exifr'

    2. Add useEffect in PhotoLightbox that extracts EXIF when currentPhoto changes:
       const [exifData, setExifData] = useState&lt;Record&lt;string, string&gt; | null&gt;(null)

       useEffect(() => {
         if (!currentPhoto?.url) return
         exifr.parse(currentPhoto.url)
           .then(data => {
             if (data) {
               setExifData({
                 dateTaken: data.DateTimeOriginal || data.DateTime || null,
                 camera: data.Model ? `${data.Make || ''} ${data.Model}`.trim() : null,
                 lens: data.LensModel || null,
                 aperture: data.FNumber ? `f/${data.FNumber}` : null,
                 shutterSpeed: data.ExposureTime ? `1/${Math.round(1/data.ExposureTime)}s` : null,
                 iso: data.ISO ? `ISO ${data.ISO}` : null,
               })
             } else {
               setExifData(null)
             }
           })
           .catch(() => setExifData(null))
       }, [currentPhoto?.url])

    3. Update the info panel's Details tab (around line 436-448) to show EXIF data:
       After the time/date display, add:
       {exifData?.camera && (
         &lt;div className="flex items-center gap-2 text-charcoal-500 text-sm"&gt;
           &lt;Camera className="w-4 h-4 text-gold-500" /&gt;
           &lt;span&gt;{exifData.camera}&lt;/span&gt;
         &lt;/div&gt;
       )}
       {exifData?.lens && (
         &lt;div className="flex items-center gap-2 text-charcoal-500 text-sm"&gt;
           &lt;span&gt;{exifData.lens}&lt;/span&gt;
         &lt;/div&gt;
       )}
       {exifData?.aperture || exifData?.shutterSpeed || exifData?.iso ? (
         &lt;div className="flex items-center gap-3 text-charcoal-500 text-sm"&gt;
           {exifData.aperture && &lt;span&gt;{exifData.aperture}&lt;/span&gt;}
           {exifData.shutterSpeed && &lt;span&gt;{exifData.shutterSpeed}&lt;/span&gt;}
           {exifData.iso && &lt;span&gt;{exifData.iso}&lt;/span&gt;}
         &lt;/div&gt;
       ) : null}
       {!exifData && (
         &lt;p className="text-sm text-charcoal-500"&gt;No metadata available&lt;/p&gt;
       )}

    4. Import Camera icon from lucide-react (line 11 already has imports)

    VERIFY: grep -n "exifr\|exifData\|camera" src/components/photo-viewer/PhotoLightbox.tsx | head -20
  </action>
  <verify>
    <automated>grep -n "exifr\|exifData\|DateTimeOriginal\|FNumber" src/components/photo-viewer/PhotoLightbox.tsx | head -15</automated>
  </verify>
  <acceptance_criteria>
    - [ ] exifr is imported at the top of the file
    - [ ] useEffect calls exifr.parse(currentPhoto.url) when photo changes
    - [ ] Info panel shows camera model when EXIF available
    - [ ] Info panel shows aperture, shutter speed, ISO when available
    - [ ] "No metadata available" shown when EXIF is missing
  </acceptance_criteria>
  <done>
    Lightbox info panel displays EXIF metadata: date taken, camera, lens, aperture, shutter speed, ISO
  </done>
</task>

<task type="auto">
  <name>Task 4: Ensure download button is wired and functional</name>
  <files>src/components/photo-viewer/PhotoLightbox.tsx</files>
  <read_first>
    - src/components/photo-viewer/PhotoLightbox.tsx (lines 359-372, the download button in bottom toolbar)
    - src/lib/supabase.ts (types for Photo.download_url)
  </read_first>
  <action>
    1. Verify the download button at lines 359-372 is correctly wired to onDownload(currentPhoto.id):
       The button already has: onClick={(e) => { e.stopPropagation(); onDownload?.(currentPhoto.id); }}

    2. Verify the Photo type has download_url field (check src/lib/supabase.ts):
       Photo interface should have: download_url?: string | null

    3. If Photo type does NOT have download_url, add it to the interface (around line 54):
       download_url?: string | null

    4. Ensure the button shows proper state when isDownloading is true (already has Loader2 spinner)

    5. The download button is in the bottom toolbar (line 327-386) - ensure it's clearly visible and functional

    VERIFY: grep -n "download_url\|onDownload\|isDownloading" src/components/photo-viewer/PhotoLightbox.tsx | head -20
  </action>
  <verify>
    <automated>grep -n "download_url\|onDownload\|isDownloading\|Download" src/components/photo-viewer/PhotoLightbox.tsx | head -20</automated>
  </verify>
  <acceptance_criteria>
    - [ ] Download button exists in bottom toolbar with Download icon and "Download" label
    - [ ] onDownload prop is called with currentPhoto.id on click
    - [ ] Button shows spinner when isDownloading is true
    - [ ] Photo type includes download_url field
  </acceptance_criteria>
  <done>
    Download button in lightbox toolbar downloads current photo at high quality
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|-----------|-------------|
| client→Supabase | Download via signed URL - untrusted URL generation on server |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | Tamper | EXIF parsing | mitigate | exifr.parse is read-only; sanitize fields before display to prevent XSS from crafted metadata |
| T-16-02 | Information Disclosure | EXIF data | accept | EXIF may reveal camera/location - this is intentional feature for photography enthusiasts |
| T-16-03 | Denial of Service | Download | mitigate | Download uses existing onDownload prop; server-side rate limiting if implemented |
</threat_model>

<verification>
## Phase-level verification

1. Build passes: `npm run build` succeeds
2. PhotoLightbox renders with all 4 enhancements:
   - Pinch-to-zoom works (touch gesture hook wired)
   - Double-tap toggles zoom
   - Swipe navigation respects zoom state
   - Info panel shows EXIF data
   - Download button calls onDownload with photo ID
3. No TypeScript errors in PhotoLightbox.tsx
4. No lint errors
</verification>

<success_criteria>
## Measurable completion

1. Pinch-to-zoom (LB-01): Pinch gesture updates zoom state between 1x and 3x
2. Double-tap toggle (LB-01): Two quick taps toggle between 1x and 2x zoom
3. Zoom-aware swipe (LB-02): When zoomed > 1x, swipe pans instead of navigating
4. EXIF display (LB-03): Info panel shows camera, date, aperture, shutter, ISO when available
5. Download button (LB-04): Download button in toolbar calls onDownload(currentPhoto.id)

## Code Quality

- [ ] TypeScript compiles without errors
- [ ] ESLint passes
- [ ] All 4 requirements implemented in single PhotoLightbox.tsx file
- [ ] Uses existing useTouchGestures hook for pinch
- [ ] Uses exifr library already in package.json
- [ ] Follows design token standards (gold-500, rounded-lg, 300ms transitions)
</success_criteria>

<output>
After completion, create `.planning/phases/16-lightbox-enhancement/16-01-SUMMARY.md`
</output>